# 2026-09-05

- 汇总跨会话知识库治理建议（已核实：ADR-0020-admin-web-ui-stack.md 为 0 字节空文件、project-status.md 内部 5+ 处互相矛盾的当前节点、行数数据过时），采纳 check_docs.py 门禁、import-linter 分层断言、收尾三同步、工具安全三级分类、portable-brief 导出等；否决 codegraph/RAG/现阶段 MCP/manifest 全量登记（防自指漂移）。
- 输出完整改造实施方案 docs/process/transformation-plan.md：S1 文档手术 → S2 check_docs → S3 CI+分层断言 → S4 依赖锁定（可并行）→ S5 脚本契约 → S6 并行试点 → S7 上帝类拆分（硬前提：CI 绿 + R0.2-07 完成）→ S8 用户端补测+契约对照；四个里程碑 M-a~M-d，每阶段带退出准则与状态跟踪表。
- **执行 S1 文档手术（含内容清洗蒸馏，语义与原账本严格一致）**：
  - project-status.md（1254 行）→ 归档切片 status-archive/2026-08.md（893 行：08-24~31 条目+截至 09-02 的结构快照）+ 2026-09.md（358 行：09-01~04 条目），原文件重写为归档账本头（70 行：提交节奏约定/术语/首个可用目标/蒸馏后「明确未完成」清单/更新模板）；历史内容原样搬运未改写，仅修复 status-archive 子目录的相对链接层级（../→../../）。
  - 新建 docs/process/CURRENT.md（44 行滚动快照）：节点 `M1 后段 / Gate 7 / OM1 / G7-R0.2-05-03 + R0.2-07`（与 roadmap V1.8 对齐）、基线 pytest 424/424 + Vitest 39/39 + Alembic 0015、最近三轮、下一步、活跃风险（Gate1 补证债/服务器 MySQL 停 0002/mypy 154 条等）、In-flight 区。
  - 新建 AGENTS.md（86 行 L0 正本：技术栈对齐实现——自研 holiday-worker 无 Celery、无 COS、LLM 仅 O17 抽取；任务类型→阅读路由表）；CLAUDE.md 改薄指针（12 行）；scripts/sync_agents_docs.py 同步脚本（--check 模式，退出码 0/1/2 契约）。注意：模板需 utf-8-sig 写入防 BOM 重复。
  - ADR 修复：ADR-0020-admin-web-ui-stack.md 补写 34 行（React 19+TS+Vite+AntD+Vitest，语义取自 R0.2-05-01B 账本记录）；ADR-0020-holiday-calendar-materialization.md 改号 ADR-0023（git mv + 文内编号说明注记 + ADR-0021 第 9 行引用同步更新）；全库无编号冲突、无 0 字节 ADR。
  - README「当前阶段」段改为指向 CURRENT.md；00-index.md 增加 L0-L3 分层说明与新会话阅读顺序（AGENTS.md→CURRENT.md→任务路由）；roadmap 状态来源口径及「每轮推进规则」、产品功能完整性审查、gate7-data-deployment-readiness-plan 的 project-status 引用同步改为 CURRENT.md。
  - transformation-plan.md S1 状态更新为已完成（含各子任务产出说明）；退出准则验证全过：新会话冷启动只需 AGENTS.md(86)+CURRENT.md(44)=130 行；「当前节点」在 CLAUDE/README/CURRENT 各至多一处且一致；全部相对链接可解析；关键事实（424/424、c2ea117、0015、72 条母集、retired 语义）与原账本逐项核对一致。
- **执行 S2 check_docs.py 文档门禁**：scripts/check_docs.py 实现 4 项检测（ADR 空文件/编号冲突、当前节点一致性——CURRENT.md 为唯一权威声明处、rules 中 ADR 引用闭环含 .claude/rules 与未来 .cursor/rules、CURRENT.md 行数预算 warning）+ S2-6 manifest 检测按计划推迟到 S5 后；契约对齐 audit_catalog_boundaries.py（fail-closed、退出码 0/2/1、--json）；tests/scripts/test_check_docs.py 8 项契约自测（monkeypatch ROOT 隔离，含故意注入节点漂移/空 ADR/编号冲突/悬空引用/超预算）；真实仓库全检 PASS + 端到端冒烟（README 注入漂移节点→exit 2 精确报不一致→还原后 exit 0）。踩坑：pytest 环境为托管 Python 3.13 需 pip install pytest；dataclass 字段需默认值；测试 fixture 写文件必须带末尾换行否则追加行拼接。
- **执行 S3 CI 流水线 + 分层断言**：
  - `.github/workflows/ci.yml` 三 job：backend（ruff check + pytest 全量 + golden cases + check_layering + check_docs）、admin-web（npm ci → typecheck → vitest → build）、frontend（npm ci → typecheck）；ruff format --check 为 non-blocking。
  - 自研 `scripts/check_layering.py`（零新依赖，AST 解析）：四条规则（domain 无框架依赖 / solver 不 import infrastructure / 依赖只能指向更内层 / interfaces 是叶子不可被 import）+ 组合根豁免名单 INFRA_EXEMPT_FILES（local_dev.py、data_governance/research_readiness.py 显式登记）；tests/scripts/test_check_layering.py 8 项契约自测；端到端注入违规验证 exit 2。
  - **ruff 存量 170→0**：ruff --fix（33 条 import 排序）+ `ruff format` 批量清偿（review.py 等 10 文件，全量 pytest 465/465 验证零语义破坏）+ B008 全局豁免（FastAPI Depends()/Query() 官方惯用法，写入 pyproject extend-ignore）+ 3 条长字符串拆分。教训：手写正则做「顶层逗号断行」会把 `"key": value` 的引号误判字符串边界导致语法损坏——**格式化一律用 ruff format，不要手写 reflow 脚本**；git checkout 回滚后再重打 P0-1 修复。
  - **修复 P0-1 安全缺陷**（架构评审最高优先级）：review.py `_SENSITIVE_REASON_PATTERN` 的 GBK 双重编码乱码导致中文凭证词（私钥/密码/令牌）拦截静默失效；对齐 service.py 正确版本 + tests/application/test_sensitive_reason_pattern.py 17 项回归（含 mojibake 特征断言防回退）。
  - F841 死赋值清理踩坑：`now = self._clock.now()` 有 8 处同名赋值但仅 3 处真死代码，第一遍正则批量删除误删 5 处在用变量 → NameError；用 symtable 全量复核后逐处恢复。教训：**同名变量批量删除必须先做作用域引用分析，ruff F841 报告的行号才是安全清单**。
  - admin-web 依赖：package-lock.json 历史上就是 0 字节空文件，npm ci 必败；npm install（npmmirror 镜像）重新生成 71KB lockfile；arborist `edgesOut` null 崩溃用 --legacy-peer-deps 绕过；补装缺失 peer `@testing-library/dom@^10.4.1`（@testing-library/react 16 强制要求）；三件套全过（typecheck/vitest 41/41/build 1.74s）。
  - frontend 依赖：npm 官方源超慢/卡死（npm ci 30 分钟无进展），切 npmmirror 后仍受 Taro 大依赖树拖累，最终由用户手动安装完成；typecheck 通过。
  - CI 排查指引 `docs/process/ci-troubleshooting.md`（三 job 本地等价命令、WinError 10055 flaky 说明、已知债务节）；S3-6 main 分支保护需用户在 GitHub Settings 手动开启（代码侧已就绪）。
  - 基线更新：pytest 465/465（424 存量 + 33 新增）、ruff 全仓 0 违规、golden 8/8、admin-web 41/41。

## S1.5 文档清洗蒸馏评审（2026-09-05）
- 结论：优先级排序 = specs/api-contract.md（编号错乱：两个 "## 12"，O09 章节插在 4/5 之间；需与 admin.py 60+ 路由核对漂移）> product/（7 处陈旧声明"管理 Web 与地点审核仍待实现"，与已实现的 admin-web 矛盾；应用代码架构设计 933 行需对照分层现状）> test/reports/ 27 个历史报告归档切片（S1 L3 模式，gate6-*.json 为机器产物）。
- 不动：decisions/（append-only，仅 1 处 superseded 引用且为模板字段）、domain/（323 行硬事实唯一来源）、ops/、research/、assumptions.md（活跃登记册）、00-index（S1 刚更新）、process/（S1 已清洗）。
- 治理建议：现状类声明（"XX 已实现/待实现"）只允许出现在 CURRENT.md，product 文档中的此类声明纳入 check_docs.py 检查或按单一权威位置原则清除。

## S1.5 立项落表（2026-09-05）
- transformation-plan.md：阶段总览表插入 S1.5 行；S1 章节后新增 S1.5 任务表（S1.5-1 api-contract 编号/路由漂移修复、S1.5-2 data-model 漂移核对、S1.5-3 product 陈旧声明清除+现状声明单一权威位置、S1.5-4 应用代码架构对照现状、S1.5-5 test/reports L3 归档、S1.5-6 gate6 json 移出 docs）；里程碑 M-a 改为含 S1.5。
- CURRENT.md：当前节点追加「S1.5 已立项」；In-flight 区同步。check_docs.py 全过，CURRENT.md 45 行。

## S1.5-1 执行（2026-09-05）
- api-contract.md V2.9→V2.10：删除误插在 §4/§5 间的第二个 "## 12 O09" 节，O09 内容回流为 §15.2 内 `#### O09`；无编号追加节（Revision 来源记录维护 / O05 预览 / O06 冲突 / 批量审核+O07）编为 15.2.1–15.2.4；§4 表后补 attraction-replacements 指引行。1477 行，check_docs 全过；已 grep 验证无外部章节号引用，无断链。
- 端点差异清单 → docs/specs/api-contract-endpoint-diff-2026-09-05.md：用户端 19/20 一致（retry P1 未实现）；管理端 doc-only 3 条（POST /admin/candidates、GET /admin/places/{id}、retirements——与 R0.2-07 发布即退役语义冲突，倾向删/改写）；实现未登记 3 条（sync-capability、dashboard-summary、GET place-revisions/{id}）；O18 草案未实现属预期。5 条裁决建议待人工确认，本轮未改代码。
- transformation-plan S1.5-1 状态已标已完成；specs/README.md 版本行同步 V2.10。

## S1.5 收尾补丁完成（2026-09-05 下午）

- **S1.5-2 data-model.md V3.0→V3.1**：头部状态行至 0001–0015（16.6 已实现）；line 195 time-preview 已开放；§13 迁移链补全 0008–0015、readiness 目标 0015；§16 导语纳入 0011；§16.3 表名修正为 publication_batches/publication_batch_items/research_snapshots（原误写 place_publication_batches）+ 发布即退役语义；§16.6 从计划改为已实现（0014/0015），sync_jobs 字段对齐实现：calendar_year（非 year）、run_lock_key 运行锁/lease 语义、ensure_builtin_holiday_calendar_seeds 种子与迁移分离。
- **S1.5-3 product 陈旧声明清除**：O18设计×2（状态行→设计意图指向 CURRENT；O17 行→已实现）、交互流程状态机、应用代码架构、信息架构 O17 行、产品功能完整性审查结论行、功能模块设计（实现状态段改设计基线表述、5.2.3、6.4.4）、管理端功能模块设计头部两行——现状类表述全部收敛至 CURRENT.md。
- **S1.5-4 应用代码架构 V1.5→V1.6**：§1 增量边界改写为「当前落地对齐」；§4.2 目录树按实际结构重写（interfaces/http/admin.py 同包、domain/admin、application/admin、infrastructure/database 按聚合拆分、holiday_sync/、runtime_config.py+composition.py/production.py/local_dev.py 组合根）；governance→admin、admin_http→http/admin.py、bootstrap/container→组合根说明；§17.2/17.3 迁移链表述指向 data-model §13 head 0015。
- **S1.5-5 reports 归档**：24 份 2026-08 报告 git mv 至 docs/test/reports/archive/2026-08/；保留 README 索引 + architecture-review-2026-09-04 + 2 份 9 月活跃报告；ops 文档 4 处路径引用同步。
- **S1.5-6 机器再生产物出库**：gate6-*.json ×5 + solver-p1-contract.json → var/reports/（var/ 已 gitignore，git rm --cached 保持本地）；6 个 scripts/run_*.py 默认输出路径同步；test/README.md（6 处）与 solver-p1-contract.md（1 处）引用同步；golden 8/8 用新路径复验通过。
- 验证：check_docs 全过、check_layering 119 文件 0 违规、golden 8/8；全量 pytest 后台复跑中。
- 教训：Edit 工具对刚被前序编辑改动的区段要先 Read 再 Edit（§4.2 二次替换失败即此因）；tail 管道会吃掉 pytest 末行统计，用 grep 过滤。

## S4 依赖锁定完成（2026-09-05，本会话；S1.5-1 由并行会话完成）
- S4-1：`uv lock` 生成 uv.lock（141 包，242KB）入库；uv 0.12.9 本机已有；`uv sync --inexact` 后全量 pytest 465/465 验证零破坏。升级政策 = ADR-0024（升级 5 步流程：改区间/lock → 全量 pytest → golden 8/8 → 差异即停写 ADR → 同提交边界；行为敏感清单 ortools/sqlalchemy/alembic/fastapi）。
- S4-2：Node engines 统一为 `node >=22 <25` + `npm >=10 <12`（采用 admin-web 的宽口径，因 Taro/Vite 均兼容 Node 23+ 不构成约束理由）；frontend/package-lock.json 用 `npm install --package-lock-only --registry=npmmirror` 再生，diff 仅 engines 4 行。
- S4-3：AGENTS.md 技术栈表逐行核对（Celery/COS/LLM 虚报 S1 已清除；后端行补 Python 版本口径 requires-python >=3.12 + CI 锁 3.12 + uv.lock 引用）；sync_agents_docs.py 重派生 CLAUDE.md。发现 S1 全部改动尚未 git 提交（81 文件待提交，含另一会话的 S1.5-1 产物 api-contract-endpoint-diff-2026-09-05.md）。
- 经验：uv lock 快（40s 解析 55 包）；`uv sync --dry-run` 先看 diff 再动手；共享账本（CURRENT.md/transformation-plan.md）在并行会话下以最后完成者收口，编辑前必须重读最新内容。

## api-contract 端点差异 6 条人工裁决落地（2026-09-05 15:20，V2.10→V2.11）

- 用户拍板：candidates 删除、place 聚合读移入计划小节；其余 4 条按建议。
- 落地明细：①删除 `POST /admin/candidates`（与 POST /places/{place_id}/revisions 重复，契约只留唯一创建路径）；②删除独立 `POST /admin/places/{place_id}/retirements`，§15.2 新增「发布版本退役语义」声明（publications 单条/批次链路发布新版时同事务原子退役，无绕门禁退役通道）；③`GET /admin/places/{place_id}` 移入新增 **§15.2.0「计划端点（未实现）」** 小节（同 O18 §15.5 模式）；④补登 3 条已实现端点：GET /place-revisions/{revision_id}、GET /dashboard-summary 入 §15.2 主表，GET /holiday-calendar-sync-capability 入 O17 小节表；⑤retry（P1）§4 表 + §8.3 标注「未排期/无路由实现」；⑥health 探针写入新增 §2.1.1 运维探针。
- 同步更新：差异清单第三节逐条标注裁决结果、specs/README V2.11、transformation-plan S1.5-1 状态、CURRENT.md（注意：CURRENT 已被另一 S4 会话更新过，合并时保留其行、追加裁决行）。
- 裁决后契约状态：用户端 §4 = 19 已实现 + retry P1 未排期；管理端 §15.2 主表全部已实现；计划端点集中 §15.2.0（1 条）+ §15.5 O18（8 条）。check_docs 全过。
