# G7-R0.2-07 O04 结构化几何证据表单验收报告

- 日期：2026-09-01
- 环境：FastAPI `127.0.0.1:8000`、管理端 `127.0.0.1:5173`
- 数据库：本地 SQLite research 环境，首批 12 条真实杭州 candidate
- 验收范围：Revision 详情的几何证据新增/编辑是否适合业务审核员使用

## 自动化结果

- admin-web Vitest：24/24 通过；
- admin-web TypeScript typecheck、production build 通过；
- 后端全量 pytest：通过；
- `git diff --check`：通过。

专项回归覆盖点几何载荷、区域自动闭合、路线最少坐标点、非法坐标中文错误、已有 GeoJSON 回填，以及列表中文摘要。

## 站内浏览器结果

登录管理端后进入真实 candidate `西湖音乐喷泉表演` 的 Revision 详情，确认：

- 点类型显示“经度”“纬度”两个业务字段，不显示 GeoJSON 文本输入框；
- 区域类型显示“边界坐标点”，逐行填写“经度, 纬度”，并提示至少 3 个点；
- 路线类型显示“路线坐标点”，逐行填写行进顺序，并提示至少 2 个点；
- 页面以中文提示坐标范围和坐标顺序，说明无需填写技术格式；
- 几何证据列表显示“点位：经度…，纬度…”等业务摘要，不直接展示原始对象；
- 编辑已有几何时，原始图形数据默认收起，展开后只读，仅供追溯；
- 控制台在本次复核中无新增 error/warning。

## Chrome 状态

本轮站内浏览器已完成上述页面复核；Google Chrome 扩展控制通道仍未建立，因此未将站内浏览器结果冒充 Chrome 验收。Chrome 通道恢复后，应在同一 research 环境重复点/区域/路线和追溯区检查。

## 结论

结构化几何证据表单已达到业务审核员可操作的最小闭环，保留技术数据追溯能力且不改变后端 GeoJSON 契约。Chrome 专项复核仍是提交前的残余验收项。
