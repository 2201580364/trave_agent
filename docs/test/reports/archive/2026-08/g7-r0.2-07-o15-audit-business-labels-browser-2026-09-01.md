# G7-R0.2-07 O15 审计中心业务可读性浏览器验收报告

- 日期：2026-09-01
- 节点：`M1 后段 / Gate 7 / OM1 / G7-R0.2-07`
- 验收对象：审计操作者账号、中文动作、服务端筛选和技术追溯详情
- 管理端：`http://127.0.0.1:5173/audit`
- API：`http://127.0.0.1:8000`
- 数据库：`.local/research.db`
- 浏览器口径：按用户确认，Codex 站内浏览器验收等同于本项目本轮 Google Chrome 验收

## 1. 验收边界

本报告验证 O15 只读审计查询的业务可读性，不修改、删除或补写任何审计事件。数据库和 API 继续保留稳定英文动作码、操作者内部编号和摘要；中文动作属于管理端展示层，登录账号由服务端正式关联返回。

## 2. 浏览器验收用例

| 用例 | 结果 | 观察 |
|---|---|---|
| 操作者主列表 | 通过 | 原 `admin_actor_*` 改为管理员登录账号 `local.admin` |
| 动作主列表 | 通过 | `ADMIN_SESSION_CREATE` 显示为“管理员登录”，`ADMIN_ACTOR_BOOTSTRAPPED` 显示为“初始化首个管理员” |
| 操作者查询 | 通过 | “操作者账号”输入 `local.admin`，服务端筛选后返回对应事件 |
| 中文动作查询 | 通过 | 动作使用可搜索中文下拉选择“管理员登录”，浏览器 URL/API 值仍为稳定 `ADMIN_SESSION_CREATE` |
| 组合筛选 | 通过 | URL 为 `?actor_login_name=local.admin&action=ADMIN_SESSION_CREATE`，结果显示本页 7 条、共 7 条 |
| 技术追溯 | 通过 | 展开事件后显示账号、中文角色“安全管理员”、操作者内部编号和动作编码 |
| 未知动作降级 | 自动化通过 | 未来未知动作主列表显示“其他管理操作”，不直接回显机器码 |
| 只读边界 | 通过 | 页面仍只提供查询、刷新、分页和展开详情，不提供编辑或删除能力 |

## 3. 自动化证据

- 后端全量 pytest：`385/385`。
- admin-web Vitest：`29/29`。
- TypeScript typecheck：通过。
- production build：通过。
- `git diff --check`：通过。
- 新增回归覆盖账号响应、账号/关键字服务端筛选、筛选后 `total`、中文动作下拉、主表不泄露内部编码和展开详情追溯。

## 4. 结论

O15 审计中心业务可读性与账号检索闭环通过。业务审核员无需理解内部 actor ID 或英文动作码即可查询和阅读审计事件，安全管理员仍可在展开详情中取得稳定技术证据。该修复不改变当前 Gate 7 主线，下一步继续首批真实候选的多地点人工审核、Projection 和批次发布。
