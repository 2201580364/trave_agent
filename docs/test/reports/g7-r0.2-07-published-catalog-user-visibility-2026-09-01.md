# G7-R0.2-07 发布地点进入用户端验收报告

- 日期：2026-09-01
- 环境：FastAPI `127.0.0.1:8000`、管理端 `127.0.0.1:5173`、用户端 H5 `127.0.0.1:10086`
- 数据库：本地 SQLite，迁移版本 `0013_backfill_solver_eligibility`
- 验收范围：管理端已发布 Projection 是否进入匿名用户端景点选择目录

## 自动化结果

- 后端全量 pytest：377/377 通过；
- 新增本地组合回归：数据库存在已发布 Projection 时，`GET /api/v1/attractions` 返回数据库发布地点和动态数据库目录版本；
- candidate、仅 human_verified、同一地点旧发布版本不进入目录；
- admin-web Vitest：16/16 通过；
- admin-web TypeScript typecheck、production build、`git diff --check` 通过。

## 浏览器结果

通过站内浏览器从用户端首页依次进入“规划杭州行程 → 选择想去的景点”，页面显示：

- `西湖音乐喷泉表演`：当前真实数据库最新发布地点，可见；
- 点击地点后状态由 `+` 变为 `✓`，已选数量由 0 变为 1，可正常选择；
- 页面未回退显示旧的 7 景点本地 Fixture；
- 未发布 candidate 未出现在列表；
- 页面控制台 error 为 0。

当前数据库还存在历史浏览器发布验收数据 `Browser E2E Revision 3`，其状态确为 published，因此按规则出现在用户端；这不是适配器泄露 candidate。后续应将浏览器验收数据迁到隔离测试数据库，避免污染人工验收目录。

## Chrome 状态

Google Chrome 已安装，浏览器扩展和本机通信配置检查正常；本轮启动 Chrome 后控制通道仍未建立，因此未能在 Chrome 内重复同一路径。站内浏览器验收已完成，但不能据此宣称本轮 Chrome 验收完成。需要重新连接/重装 Codex Browser 插件后补做 Chrome 复核。

## 结论

“管理端发布成功 → 用户端目录可见并可选择”的代码和站内浏览器闭环通过。生产环境仍使用严格不可变 JSON 发布快照和真实 OD；本地数据库适配器使用明确标记的近似 OD，不放宽生产门禁。
