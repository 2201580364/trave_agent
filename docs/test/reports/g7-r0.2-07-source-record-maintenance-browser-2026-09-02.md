# G7-R0.2-07 来源记录维护浏览器验收记录

更新时间：2026-09-02  
产品里程碑：M1 后段  
管理侧阶段：Gate 7 / OM1  
验收环境：本地 API `127.0.0.1:8000` + admin-web `127.0.0.1:5173`，SQLite `.local/research.db`，站内浏览器

## 已完成检查

- `/health/live` 返回 `{"status":"ok"}`。
- `/health/ready` 返回 `ready=true`，迁移版本为 `0013_backfill_solver_eligibility`。
- 管理员登录成功，候选清单可以按“浙江省博物馆”筛选并进入修订详情。
- 修订详情出现独立“来源证据”区域；来源渠道、渠道类型、来源决策、采集方式、来源地址和观察时间均以中文业务标签展示。
- “新增来源记录”弹窗字段完整：来源渠道、具体来源地址、观察时间、采集方式；内容哈希和补充说明收在“高级追溯信息（通常无需填写）”中。
- 来源渠道下拉只展示已治理且允许地点事实的渠道；和风天气渠道不会出现在列表中。
- 选择“杭州西湖风景名胜区管委会公开网站”后，页面展示该渠道允许域名、条件和采集方式提示。
- 已有关联 active 几何证据的来源，其“从当前修订移除”按钮处于禁用状态，页面提示需先将子证据改用其他来源。
- 前端修订详情的描述表格已移除固定跨列设置，避免窄屏下 `Descriptions` 列跨度告警。

## 自动化验证

- 后端：全量 `pytest` 通过。
- 前端：`npm run typecheck` 通过。
- 前端：Vitest `8 个测试文件 / 32 个测试通过`。
- 前端：`npm run build` 通过。
- `git diff --check` 通过。

## 写入闭环结果

在用户确认本地测试写入后，已完成以下操作：

1. 通过管理端新增一条“杭州西湖风景名胜区管委会公开网站”来源记录，使用官方 HTTPS 页面、人工查阅方式。
2. 页面刷新后来源记录出现在列表，中文显示渠道、渠道类型、来源决策、采集方式、地址和观察时间。
3. 服务端数据库确认 `revision-hz-cand-007` 的 `revision_version` 从 1 递增到 2，来源记录已加入当前 Revision 的关联列表。
4. O04 几何证据来源下拉显示新来源的“中文渠道名 · 观察日期”，未暴露内部来源编号作为业务主展示。
5. 数据库确认追加 `PLACE_SOURCE_RECORD_CREATED`、目标类型为 `place_source_record`、结果为 `succeeded` 的审计事件。
6. 新来源尚未被子证据使用时可以解除当前 Revision 关联；已有 active 几何来源仍保持移除按钮禁用，引用保护逻辑已在页面和自动化测试中验证。

本能力域当前状态为“代码、自动化测试、浏览器写入闭环和审计核对均完成”，可以进入提交评审。
