# G7-R0.2-05-01B 管理 Web 验证报告

- 日期：2026-08-29
- 节点：O00 管理员登录、O16 管理员与角色、O15 最小审计只读面
- 结果：工程门禁通过；Chrome 桌面登录页与未登录路由保护已验收，窄屏覆盖待补

## 已验证

| 范围 | 结果 | 证据 |
|---|---|---|
| admin-web TypeScript | 通过 | `npm run typecheck` |
| API client 安全边界 | 通过 | Vitest 3/3；登录不发送旧 Authorization、凭证只从内存读取、401 清理会话 |
| 生产构建 | 通过 | `npm run build`；Vite 路由 chunk 构建成功 |
| 后端回归 | 通过 | `py -3.12 -m pytest -q`，340 项通过 |
| 本地静态入口 | 通过 | Vite `http://127.0.0.1:5173/` 返回 200 |
| 本地 API 进程 | 已启动 | `scripts/run_local_dev.py --port 8000`；健康路径需按后端现有路由检查，根路径 404 不代表进程不可用 |
| Chrome 桌面登录页 | 通过 | `http://127.0.0.1:5173/login` 实际渲染；标题、表单、密码遮罩、登录按钮和安全提示可见，截图无溢出 |
| Chrome 未登录路由保护 | 通过 | 访问 `/audit?result=rejected` 自动回到 `/login`，没有泄露审计内容 |
| Chrome 管理员登录联调 | 通过 | 使用本地一次性 bootstrap 管理员登录；O16 列表、O15 审计事件、退出和刷新后重新登录均符合预期 |

## 页面实现范围

- O00：独立管理员登录、稳定失败提示、内存 Bearer token、会话到期回登录、安全退出。
- O16：管理员列表、创建管理员、角色调整、理由字段、乐观锁冲突提示和目标登录名二次确认；按钮按权限过滤，但服务端权限仍是事实来源。
- O15：按 actor/action/target/result 筛选、URL 状态、手动分页、digest 展开详情；只读，不提供编辑、删除或敏感原文导出。
- 未实现 O01–O08 地点审核和 O09 发布中心，页面不会伪造审核指标或发布数据。

## 暂缓项

本轮 Chrome 已完成桌面登录页、未登录路由保护、真实管理员登录、O16 管理员列表、O15 审计查询、主动退出和刷新后会话清理验收。当前 Chrome 控制能力不提供 viewport 覆盖，因此未强制执行 375×812 截图；角色写操作未执行，避免为验证引入不必要的管理员权限变更。一次性 bootstrap 凭证已在成功创建管理员后从 `.env` 移除。

## 风险与后续

- Ant Design 首屏已通过路由懒加载拆分；后续 O01–O09 页面继续按路由拆分，避免治理工作台扩大首包。
- 本地 `.local/travel_agent.db` 仍是旧 0005 数据库，未用它宣称管理登录可用；应通过 `scripts/run_local_dev.py` 迁移到 0007 后再配置一次性 bootstrap 管理员进行联调。
- 受控服务器 MySQL/Redis 未连接或迁移；R0.2-05-02 完成前不进入服务器正式部署。
