# 项目状态归档账本（历史记录）

> **本文件是归档账本**：只按月追加新轮次记录，永不全文读。**当前状态看 [CURRENT.md](CURRENT.md)**；跨里程碑稳定路线看 [project-roadmap.md](project-roadmap.md)。
>
> 历史切片：[status-archive/2026-08.md](status-archive/2026-08.md)（2026-08-24～31 + 截至 2026-09-02 的结构快照）· [status-archive/2026-09.md](status-archive/2026-09.md)（2026-09-01～04 续接记录）。

## 提交节奏约定

提交边界按“可验收能力域/里程碑切片”划分，而不是按单个 API、字段或页面按钮划分。一个提交至少应同时包含对应的后端契约、权限/审计、前端流程（如适用）、自动化测试、验收证据和状态文档；只有完成该能力域的最小闭环后才判断适合提交。未完成的连续工作保留在同一工作区，避免产生难以回滚和审阅的过细提交。

## 术语

| 术语 | 含义 |
|---|---|
| `M1–M4` | 产品路线里程碑；M1 是最小可用产品，M2 是信息与决策增强，M3 是全流程出行服务，M4 是记忆与生态 |
| `OM1–OM4` | 管理侧能力里程碑；依次支撑研究数据/发布、多目录/媒体、社区/实时服务、共创/AI 内容治理 |
| `P0` | 当前里程碑的阻塞优先级；缺失时无法形成首个可用闭环或无法验证核心假设 |
| `P1` | 当前里程碑的重要优先级；MVP 应具备，但可以晚于首个纵向切片 |
| `P2` | 增强优先级；不阻塞当前里程碑，可在证据充分后实现 |
| `G0–G7` | 假设驱动开发的证据 Gate，不等同于产品路线或实现优先级 |

历史机器标识 `solver-p1-v1`、`parameters-p1-*` 和 `DEFAULT_SOLVER_P1_CONTRACT` 必须保留；其中的 `p1/P1` 不再表示产品阶段命名。当前结构契约已按 ADR-0011 升级为 `solver-p1-v2 / trip-result-v2`；参数按 ADR-0013 保持 `parameters-p1-2026-08-26`，单日间节点下午展开语义按 ADR-0015 升级为 `constraints-p1-v5`；历史版本仅用于不可变 Revision 回放。

## 首个可用目标

```text
打开浏览器
→ 确认杭州，选择旅行日期与到达/离开时间
→ 选择或确认到达交通方式；返程方式默认显式继承
→ 系统自动预留进城接驳、提前到站和末景返程时间，用户可按需调整
→ 浏览并勾选景点
→ 确认摘要，默认使用“适中”节奏；同行人群与高级设置可选
→ 生成行程
→ 查看每日时间线、交通、晚餐、天气、时段偏好、未排入项与降级原因
→ 按需替换单个景点并生成同一 Trip 的新 Revision，旧版本继续保留
→ 从“我的行程”恢复当前版本，并可只读回看历史 Revision
→ 将当前 Revision 生成为脱敏计划分享，访客可只读查看或复制景点为自己的新草稿
→ 保存本次结果并提交结构化整体/节点反馈
```

## 明确未完成（截至 2026-09-04，最终有效口径）

> 详细的已完成证据与逐条历史记录见归档切片。以下为从全量账本蒸馏出的当前有效「未完成」清单：

- **外网生产服务**：FastAPI/Taro 尚未作为外网生产服务启动；域名/TLS、反向代理、应用进程安全加固、集中监控、异地备份、镜像 digest、Python 生产 lock 未完成。服务器真实 MySQL 仍停在已验收的 `0002` 基线，下次发布前须依次执行 0003→0015 并重新验证 readiness/备份/隔离恢复。
- **Gate 7 证据链**：首个真实 `locked` 环境尚未生成；外部专家/用户证据、H3 结论均未开始（H1–H12 全部「未验证」）。G1 存在真实用户研究补证债务（无真实访谈纪要），须在 M1 最终决策前完成 8–10 名目标用户发现研究。R0.2-06 按需 OD 子图、R0.3 服务器全栈 Compose、R0.4 内部 dry run 均待执行。
- **研究数据规模化**：72 条候选中仅 3 条 published；12 条人工审核批次待补证与 reviewer 审核；50–75 条研究目录、真实 candidate 批量「勾选→预览→执行」UI 回归未完成。
- **M1 产品口径**：到离交通、节奏/同行人群、景点筛选仍是首切片简化实现；午晚餐仍是软留白（具体餐厅节点未实现）；M1 收口主队列工程完成 4/4 但必须先过 Gate 7，不能仅凭工程完成跳转 M2。
- **增强项未做**：计划分享的 PNG/JPEG 导出、二维码/小程序码、保存相册、原生分享、作者撤回、转化埋点（A6-9.3.1）；账号行程与草稿/失败 Intent 并入列表；M2–M4 各功能仅产品设计。
- **技术债**：全项目 strict mypy 基线 154 条/27 文件未清零；全仓 Ruff 历史格式债；H5 入口约 304 KiB 体积警告（P1）；三日之外正式气候基线 Provider 未实现。

## 每轮结束更新模板

```text
更新时间：YYYY-MM-DD
产品里程碑：M-x
当前阶段：A-x / G-x
本轮完成：
- ...
验证结果：
- ...
当前进度：
- ...
遗留问题：
- ...
下一步：
- ...
```

> **使用说明**：自 2026-09-05 起，每轮结束不再把全量记录追加到本文件头部，而是：① 更新 [CURRENT.md](CURRENT.md)（当前节点/最近完成/下一步/风险/In-flight）；② 仅当本轮产生需要长期留痕的历史结论时，在本文件末尾按上述模板追加一条简短记录；③ 月度切片由 S2 的 check_docs.py 维护。
