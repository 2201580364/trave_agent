# 状态归档切片：2026-09（上半月）

> 历史账本切片，只读存档。覆盖 2026-09-01 至 2026-09-04 的续接记录。文中「当前节点」均为当时快照，最新状态见 [../CURRENT.md](../CURRENT.md)。内容自原 project-status.md 原样搬运，未改写语义。

### 2026-09-03 续接进度（G7-R0.2-05-03 / R0.2-07：多地点审核基线）

O17 已提交后，主线回到杭州研究数据扩充与多地点回归。阶段编号已校正：研究数据审核和批量发布属于 `G7-R0.2-05-03 / R0.2-07`；`G7-R0.3` 仍专指后续受控服务器 H5 全栈 Compose 部署，不能用来命名当前数据准备工作。

只读核对 `.local/research.db` 得到当前真实母集：72 条杭州目录 Revision，其中 `published=3`、`candidate=69`、`human_verified=0`，目录边界审计无违规。3 条已发布数据为平湖秋月、浙江省博物馆孤山馆区和西湖音乐喷泉表演；69 条候选均仍需补证。现已新增 `report_research_readiness.py`，直接复用管理端六项审核准备度，以 JSON 或中文 Markdown 输出生命周期、缺失证据、待 reviewer 审核和逐地点明细；该工具只读，不会改变审核、求解资格或发布状态。

基线检查同时发现并修复两个批量审核前的门禁缺口：编辑基础事实或新增几何、访问点、开放时间后会清除对应的导入期待核验标记；Revision 级“审核通过”现在必须满足六项准备度全部已采集且已核验，不再允许仅因“现有证据均已审核”而把缺少访问点、开放时间或关系结论的候选推进为 `human_verified`。Projection publication check 继续作为独立第二道门禁。定向后端回归 `40/40`、后端全量 `432/432`、管理端 `41/41`、TypeScript typecheck 和 production build 均通过。

已选定下一轮 12 条代表性人工审核批次，覆盖普通点状景点、景区、街区、步行路线、市集、游船体验和固定场次表演：`hz-cand-003/006/010/013/014/020/024/027/042/045/048/052`。当前批次 12 条均为 candidate：基础信息、访问点、开放时间和关系检查均待完成；其中 7 条点状记录已有候选几何待 reviewer 核验，5 条非点实体还需补真实 area/route 几何。基线和操作顺序见 `docs/test/reports/g7-r0.2-05-03-research-readiness-baseline-2026-09-03.md`。

当前节点：`M1 后段 / Gate 7 / OM1 / G7-R0.2-05-03 + R0.2-07`。当前工作是先完成上述 12 条的人工补证与 reviewer 逐项审核，再准备 Projection、批次发布和用户端可见性回归；稳定后扩展至研究最低目录 50–75 条，之后实施 R0.2-06 按需真实 OD 子图，再进入 R0.3 服务器部署。

### 2026-09-04 版本退役语义修复（R0.2-07）

发现同一地点发布第 2 版后，第 1 版仍显示为“已发布/求解可用”。根因是发布事务只激活新版本，没有退役同一 `place_id` 下的旧 Revision 和 Solver Projection。现已在发布边界增加原子退役：同一地点的新版本发布成功时，所有旧的 `published` Revision 转为 `retired` 且 `solver_eligible=false`，对应旧 Projection 转为 `retired`；历史详情仍保留，但不会进入用户端求解目录。当前 `.local/research.db` 已备份并修复浙江省博物馆孤山馆区旧第 1 版，只有第 2 版保持可用。新增持久化回归覆盖“发布新版本后旧版本退役”。

### 2026-09-03 续接进度（G7-R0.2-09：O17 可执行同步闭环已提交）

提交记录：本能力域已提交为 `c2ea117 feat(gate7): 完成节假日历同步与审核治理闭环`。
本次提交包含 O17 节假日历同步完整闭环、O06 来源冲突判定修复、O07 reviewer 审核入口与进度修复、迁移、自动化测试及配套产品/规格/状态文档。提交前后 `git diff --check` 均通过；用户已完成真实 2024 年同步的站内浏览器人工验收。

用户已完成节假日历同步功能人工验收：同步任务创建、AI/系统执行过程流、预览结果查看、预览数据调整、确定性校验以及确认入库发布路径均已实际走通，未发现新的业务阻断。结合后端全量 `428/428`、管理端全量 `41/41`、类型检查、生产构建和 API readiness 验证，O17 已达到本轮“大模块提交”条件，并已与 O06 来源冲突误判修复、O07 reviewer 审核入口修复一起由提交 `c2ea117` 固化；提交后主线回到 G7-R0.2 多地点研究数据准备。

针对 `revision-hz-cand-004` 的人工验收继续修正 O07 交互：此前页面已显示关系“已裁决”，但 reviewer 的关系证据审核状态仍为“候选”，且 O07 没有 reviewer 操作入口，形成“有阻断、无处理入口”。现已为 O07 关系表接入与 O04/O05 一致的 reviewer 逐项“通过/驳回”按钮；数据编辑员仍使用独立“裁决”按钮。发布准备区明确提示两者差异并定位 O07，避免把关系裁决误认为证据审核。当前平湖秋月关系 `hz-rel-013` 应先由 reviewer 完成逐项审核，再执行 Revision 级审核通过。管理端 O07 定向测试 `12/12`、TypeScript 检查通过。

补充修复 O07 进度口径：`关系裁决（O07）` 现在按 `resolution_status` 统计“已裁决/总数”，不再把 reviewer 的 `review_status` 误显示为裁决进度。对已完成关系裁决但尚未完成 reviewer 逐项审核的记录，发布准备区新增“地点关系证据尚未审核”阻断项，并直接定位 O07；这样“1/1 已裁决”和“审核列仍为候选”可以同时成立且含义清晰。真实 `revision-hz-cand-004` 当前正是该状态：关系 `hz-rel-013` 已裁决，但 reviewer 尚未点击 O07 表格中的“通过/驳回”。管理端定向测试 `12/12`、TypeScript 检查通过。

人工审核 `revision-hz-cand-004` 时发现：地点关系已裁决，但页面仍以“存在未完成裁决的来源冲突”阻断审核。数据核对确认该 Revision 只有一条有效来源记录，不存在同一来源的多内容版本；根因是前端、审核准备度和 Projection 发布门禁直接把修订级 `conflicts_resolved=false` 当成真实冲突。该字段会在编辑证据后重置，因此不能单独证明冲突存在。

O06 判定现已统一为“先根据来源记录计算是否存在同一 `source_id` 的不同内容指纹，再在确有冲突时检查 `conflicts_resolved`”。无实际冲突自动通过并显示“未发现”，不再要求数据编辑员执行形式化确认；真实冲突仍须裁决，且 O06 与 O07 地点关系裁决继续保持独立。后端审核准备度、reviewer 通过门禁、Projection 发布门禁和管理端发布摘要复用相同语义。回归覆盖“无冲突 + 旧字段 false 可通过”和“真实多版本冲突仍阻断”；后端全量 pytest `428/428`、管理端 Vitest `40/40`、TypeScript typecheck 和 production build 均通过。API 已重启且 readiness 为 `ready`。浏览器刷新后因管理会话仅保存在页面内存而回到登录页，本轮未重新输入凭证；登录后仍需在 O07 对已裁决但 `review_status=pending` 的 `hz-rel-013` 执行 reviewer 逐项审核，随后才能执行 Revision 级“审核通过”。当前节点仍为 `M1 后段 / Gate 7 / OM1 / G7-R0.2-09`。

O17 已从“确定性内核/API”继续推进到可部署执行边界。生产组合现已接入中国政府网严格公告发现器、受限域名与内容获取器、供应商中立的 OpenAI-compatible 结构化模型适配器，以及独立 `travel-agent-holiday-worker` 进程。Worker 支持有限批次、周期轮询、优雅停止、running lease 恢复、同年度运行锁和指数退避；日志继续使用项目既有“按组件最多一级目录、debug/info/error 分级、每日文件、按月 ZIP 归档”策略，不向标准输出集中倾倒业务日志。

AI 适配器只产生 candidate 结构，所有 `evidence_quote` 必须逐字存在于官方正文，之后仍要通过年份、地区、日期范围、重叠、调休冲突、七类节日完整性、内容哈希和 normalized digest 等确定性门禁。模型网络失败被标记为 `temporarily_unavailable / extraction_service_unavailable`，不会冒充“该年度公告尚未发布”，也不会绕过发布事务。`.env.example` 已补齐 Worker、轮询、批量、模型 Base URL/名称/API Key/超时配置；真实密钥不得提交。

O17 管理页面已形成首版业务闭环：展示 published 年度、最近 Job、中文状态和结果；支持选择年度与“仅预览/同步并发布”；存在 queued/running Job 时自动轮询；提供 Job 详情、官方公告链接、重试时间和 Calendar Version 的节假日/调休原文证据详情。只有 discoverer、fetcher、extractor 和独立 Worker 均配置完成时按钮才开放；服务端也会拒绝在执行服务未启用时创建永久排队任务。该闭环已完成用户人工验收。

真实来源只读验证已确认：2026 年可严格发现并获取中国政府网正式公告；2027 年在官方搜索正常响应下未发现严格匹配公告，应进入 `not_announced`，网络异常仍走独立失败路径。自动化现为后端全量 `428/428`，管理端 Vitest `41/41`、TypeScript typecheck、production build 通过；新增范围 Ruff 通过，仓库全量 Ruff 仍存在历史格式债务，未在本能力域扩大修改。真实 2024 节假日历同步人工验收已完成。

O17 完成时节点仍为 `M1 后段 / Gate 7 / OM1 / G7-R0.2-09`。Calendar 版本影响查询已实现：`0015_holiday_exception_provenance` 为物化日期例外保存日历版本来源，接口返回日期差异、受影响地点，并明确排除无来源历史记录。其后主线回到 `G7-R0.2-05-03 / R0.2-07` 受控研究数据扩充和多地点回归。

本轮已确认 `.env` 四项 O17 配置均存在，API 已重新启动并报告 `0015_holiday_exception_provenance / ready=true`，独立 Worker 已以前台周期模式运行。由于页面当前会话未登录，未代替用户输入管理员密码；后续应登录 `/holiday-calendars`，确认同步按钮已开放，再以 2026 年“仅预览”创建一条任务并观察 Worker 处理结果。同步发布模式必须在 preview 结果检查后另行操作。

随后发现本地浏览器使用的 `build_local_dev_app()` 未注入 O17 环境设置，导致即使 `.env` 配置齐全，capability 仍返回不可执行。现已修复本地组合，新增回归确认配置完整时 `execution_available=true`；API 重启后已加载该修复。管理端页面刷新并重新登录即可看到可用按钮。

本轮针对“创建同步任务后无反馈、无法查看进度、无法中断”完成异步任务可观察性增强：新增 `cancelled` 终态和 `POST /api/v1/admin/holiday-calendar-sync-jobs/{job_id}/cancel`，仅允许取消 queued 或等待重试任务；running 任务不强制终止，避免半成品数据。管理端创建后立即打开任务详情，自动轮询 queued/running 状态，显示处理阶段、尝试次数、结果/错误说明、重试时间和取消入口；完成态显示明确中文结果。取消操作写入管理审计，Worker 不会领取已取消任务。

本轮全量后端 pytest 为 `422/422`，新增取消状态定向测试通过；管理端 Vitest `35/35`、TypeScript typecheck、production build 继续通过。新增 API 已写入 O17 文档与接口类型。当前 API 进程需重启后才能提供取消端点，前端刷新后即可使用。

### 2026-09-03 续接进度（进入 G7-R0.2-09：O17 确定性同步内核）

首批地点人工审核暂未发现新问题，且 O05/O07 已由提交 `70497be`、`435d617` 固化，当前执行节点正式从 `G7-R0.2-08` 切换到 `G7-R0.2-09 中国法定节假日历自动同步与版本治理`。

本轮已完成 O17 第一实现切片：新增 Alembic `0014_holiday_calendar_sync`，建立年度日历、节假日区间、调休工作日和同步任务四张表；新增不可变 Calendar Version、Sync Job 状态机、官方域名/年份/范围/重叠/放假与调休冲突/节日完整性/证据片段/稳定 digest 等确定性校验；新增 SQLAlchemy 仓储、同内容幂等、内容变化创建 v2 并替代 v1、operation intent 幂等，以及发布与管理审计同事务写入。统一 `ChinaHolidayCalendarSyncService` 已通过可注入 discoverer/fetcher/extractor 端口用 fake adapter 跑通，不依赖真实网络或 AI。

状态语义补充 `validated_preview`：仅表示 preview 模式校验通过但未发布，不能冒充 `up_to_date`，也不能供 O05 使用。测试已覆盖正常发布、重复同步、版本替代、公告尚未发布、网络/TLS 暂不可用、无效抽取阻断、预览不写库、任务状态和迁移 head。O17 与迁移定向回归 `14/14`、新增范围 Ruff 和 diff 检查通过；全量 pytest 首次为 `403 passed, 1 failed`，唯一失败是 Windows 创建事件循环套接字时的 `WinError 10055`，隔离复跑该既有用例 `1/1` 通过，详见测试报告。当前尚未实现管理 API、O17 页面、O05 数据库目录切换、真实中国政府网适配器、AI 抽取、周期任务、AI Tool 和影响查询，不能将第一切片描述为 O17 完成。

当前节点：`M1 后段 / Gate 7 / OM1 / G7-R0.2-09`。下一步先补管理 API、权限与完整同步审计，再让 O05 只读取数据库 `published` 日历并提供受控内置日历种子迁移；随后完成 O17 管理页面。以上闭环稳定后，最后接真实官方公告发现与 AI 抽取并进行站内浏览器验收。

第二实现切片已继续完成：新增同步 Job 创建、列表、详情和日历版本详情管理 API；创建任务要求数据编辑权限，查询要求管理只读权限，operation intent 保持幂等，任务入队写入 `HOLIDAY_CALENDAR_SYNC_QUEUED` 审计。生产组合根已接入数据库 Holiday Calendar Catalog，O05 的日历列表、日期预览和节假日例外生成不再直接读取代码常量，只消费数据库 `published` 版本。已核验的 2025/2026 内置目录通过幂等受控种子写入版本表；已有同年度 published 版本时不会覆盖。未迁移数据库不会在应用模块导入时因缺表崩溃，readiness 仍会明确报告迁移落后。

第二切片回归结果：后端全量 pytest `406/406`；admin-web Vitest `34/34`、TypeScript typecheck、production build 和 `git diff --check` 全部通过。审计中心已补充“创建法定节假日历同步任务 / 发布法定节假日历版本”的中文动作和目标类型。当前尚缺真实官方来源适配器、异步 Worker/周期任务、AI 抽取、影响查询和 O17 页面；在 Worker 可实际推进 Job 前不提前展示一个会永久停留在“排队中”的同步按钮。下一步先实现真实/可替换 Worker 执行边界和官方来源发现获取，再建设 O17 页面并做浏览器验收。

### 2026-09-03 续接进度（O07 关系端点名称与来源闭包二次修复）

针对 `revision-hz-cand-063` 仍显示“待补充地点名称 → 待补充地点名称”的问题，进一步修复关系名称解析：每个端点现在独立按 `place_id` 或 `place_revision_id` 查询最新 Revision，兼容历史关系导入格式；关系来源查询同时覆盖双方地点，避免关联地点来源被误报缺失。接口新增 `relation_summary`，前端保留双方名称、中文关系含义、关系说明和识别依据展示。

验证结果：admin-web Vitest `34/34`、TypeScript typecheck 和 `git diff --check` 通过；当前 API 健康检查 `GET /health/live` 返回 `200`。由于工作区专用 Python 未安装 pytest，本轮未执行后端 pytest；需重启当前 API 进程后刷新 `revision-hz-cand-063` 完成浏览器视觉复核。当前仍处于 `M1 后段 / Gate 7 / OM1`，本修复待浏览器复核后与 O07 能力域合并提交。

随后将 O07 裁决选项从内部状态文案改为业务动作文案：`resolved` 显示“同意此地点关系”，`not_required` 显示“驳回此地点关系”，`pending` 显示“待处理”。弹窗增加同意/驳回的实际影响说明；数据库枚举和发布门禁契约保持不变。前端测试 `34/34`、typecheck 和 diff 检查通过。

人工提交关系裁决时发现 422：后端要求 `resolved` 必须提供 `decision_note`，前端此前允许空说明提交。现已在保存前统一要求同意或驳回均填写判断依据，并提供中文错误提示；未改变后端状态契约。

随后通过接口响应定位到第二个 422 根因：关系裁决服务保存时误将关系 `review_status` 写为不存在的 `pending`（`pending` 只属于裁决状态），导致下一次加载关系实体时报“关系审核状态无效”。现已改为保存为 `candidate`，并对早期历史关系中的未知审核状态做兼容归一为 `candidate`；裁决结果仍由独立的 `resolution_status` 表达。需重启 API 后重试 `hz-rel-013`。

### 2026-09-03 续接进度（O07 关系双方名称与语义展示修复）

人工验收发现 O07 关系列表仅显示“当前地点 → 关联地点”，无法判断具体景点和关系方向。已修复管理端证据组装：关系端点名称现在同时按 `place_id` 和历史/候选 `place_revision_id` 解析，避免旧批次端点导致名称为空；关系来源闭包也会读取双方地点的来源记录，不再把关联地点的有效来源误报为“来源记录不可用”；无法解析时明确显示“待补充地点名称”，不再使用泛化占位文案。

前端关系列表新增双方真实名称、关系类型中文含义、关系说明和识别依据列，内部端点 ID 仅保留在悬停追溯信息中。O07 文档同步规定了可裁决的最小展示信息，并给出“西湖 → 断桥残雪”的完整示例。

验证结果：admin-web Vitest `34/34`、TypeScript typecheck、production build 和 `git diff --check` 通过；后端 pytest 未能执行，当前环境未发现可用 Python 安装。尚未进行登录后的浏览器视觉复核。本轮仍属于 M1 后段 / Gate 7 / OM1 的 O07 可解释性修复，待浏览器复核后与现有能力域合并提交。

### 2026-09-02 续接进度（O18 数据采集设计规格收口）

用户要求先完善数据采集设计，本轮未实现采集程序。已新增并接受 [ADR-0022：地点数据采集进入 staging，关系线索与人工治理分离](../../decisions/ADR-0022-data-collection-staging-and-relation-clues.md)，并将 O18 的边界同步到 API 契约、数据模型和用户功能树。

本轮明确了后续实现必须遵守的最小闭环：`采集批次 → staging 结果/差异 → candidate Revision/SourceRecord/RelationClue → O04–O07 人工审核 → Projection/Publication Gate`。采集批次、请求尝试、结果和关系线索的数据结构、幂等键、失败清单、回放字段、权限、错误码和审计边界已写入权威规格；当前仍没有对应 Alembic 迁移或通用采集 API，不能将“设计完成”误认为“功能已实现”。

当前节点：`M1 后段 / Gate 7 / OM1 / G7-R0.2-08`。O18 处于“已设计、待实现”；下一主线为先完成现有 O05/O18 前置验收与大模块提交，再进入 `G7-R0.2-09` 中国法定节假日同步或 `G7-R0.2-05-03` 杭州单城市受控采集批次，后续在 `M2/OM2` 扩展多来源、多城市和 AI 辅助抽取。

文档验证：已检查 O18、API 契约、数据模型、功能模块、ADR 索引和路线状态的交叉引用；本轮为文档变更，无代码测试要求。工作区仍包含前序未提交改动，暂不单独提交。

### 2026-09-02 续接进度（全天开放语义与 O07 关系线索导入修复）

数据采集平台的统一设计已补档至 [O18 地点数据采集与关系识别设计](../../product/O18地点数据采集与关系识别设计.md)：明确来源准入、批次状态、限流/缓存/失败、归一去重、Revision 生成、关系线索、AI 辅助抽取、管理端工作台、API 草案、权限边界和 M1→M2 实施顺序。O18 当前仅为规划，不提前实现通用爬虫；M1 先完成杭州受控候选采集与人工审核闭环，M2/OM2 再扩展多来源、多城市和 AI 抽取。

本轮修复了候选目录与研究库之间的关系证据断链。业务上，全天开放地点应勾选 Revision 的“全天开放”开关，`time_rules` 可以为空；不应填写周一至周日 `00:00–23:59` 伪造普通时间窗。编辑弹窗已增加黄色“!”悬停提示，明确固定闭馆日和日期例外仍需单独维护。

O07 根因已确认并修复：杭州候选目录从 11 组扩展为 15 组确定性关系线索，补充西湖→断桥残雪、平湖秋月、曲院风荷、三潭印月四条 `contains` 候选；候选导入脚本现在会在两端地点均已导入时幂等写入 `place_relations`，部分导入缺少另一端时延迟处理，且不会覆盖既有人工裁决。当前 `.local/research.db` 已先完成带时间戳备份，再补录 15 条待审核/待裁决关系；西湖对应的 6 条关系（含原有湖滨公园、苏堤春晓）已可在 O07 展示。

O06 当前按治理边界自动检测同一来源标识下多条记录的内容指纹不一致；无具体冲突时自动通过，不要求数据编辑员执行形式化确认。不同来源对地址、坐标、开放时间等事实值的字段级比对暂不由内容指纹冒充，仍在 O04/O05 逐项核验。

关系裁决还补齐了两端可操作性：关系同时出现在容器和子地点的证据中时，任一端地点均可提交同一条裁决，避免子地点“看得到但无法处理”。

自动化验证：候选目录/导入回归 `12/12` 通过（目录哈希、覆盖矩阵、15 条关系导入、部分导入延迟、幂等和 dry-run）；全量后端 pytest、admin-web Vitest `34/34`、TypeScript typecheck、production build 和 `git diff --check` 均通过。站内浏览器已打开西湖修订详情，但现有管理会话已过期并回到登录页，未代填敏感凭证，登录后 O07/全天开放视觉验收待人工登录后复核。当前节点为 `M1 后段 / Gate 7 / OM1 / G7-R0.2-08`，本轮能力域代码、研究库补录和自动化质量门禁已收口，建议完成登录后只读视觉检查，再与同一工作区的前序改动合并为一个大模块提交。

### 2026-09-02 续接进度（中国法定节假日历自动同步决策）

用户已接受“中国大陆年度法定节假日历由 AI 辅助自动同步”的方案。新增 ADR-0021 和 O17 产品设计，确定管理端同步按钮、后台周期任务与 AI Tool 共用同一个 `ChinaHolidayCalendarSyncService`：AI 负责官方公告发现、内容理解、结构化抽取和原文证据定位；确定性程序负责官方来源白名单、年份、日期冲突、完整性、幂等、不可变版本、审计和发布门禁。正常路径不要求业务人员逐年录入或审批，但 AI 无权直接写 published。

状态明确区分 `not_announced`、`temporarily_unavailable`、`needs_attention`、`published/up_to_date` 和 `superseded`。只有官方发现成功且未找到正式公告时才能返回“尚未发布”；网络、TLS 或超时不得伪装为未发布。O05 只读取 published 日历；官方变化创建新版本，不回写历史 `PlaceDateException`、Projection、TripRevision 或研究快照。

本轮只完成设计、ADR、产品/契约/数据/领域/核验流程和路线登记，O17 数据库、同步 Job、官方来源适配器、AI Tool、周期任务和页面尚未实现。当前节点仍是 `M1 后段 / Gate 7 / OM1 / G7-R0.2-08`；先完成现有 O05 登录后浏览器视觉验收和提交，再进入 `G7-R0.2-09 中国法定节假日历自动同步与版本治理`。该工作包初始优先级为 P1；当允许规划日期跨入缺失年份时自动升级 P0，并应在 R0.3 受控部署前完成中国单一地区闭环。

### 2026-09-02 续接进度（O05 节假日生成 422 修复与 Revision 详情视觉收口）

当前节点保持 `M1 后段 / Gate 7 / OM1 / G7-R0.2-08`。人工核验在 `revision-hz-cand-007` 执行“按节假日历生成”时返回 422，已定位为审计目标类型映射缺口：业务生成本身能够计算日期例外，但成功提交前写入 `PLACE_HOLIDAY_EXCEPTIONS_GENERATED` 时，审计层只识别单条 `DATE_EXCEPTION`，未识别批量动作中的 `HOLIDAY_EXCEPTIONS`，错误被统一包装成 `domain_validation_failed`。现已将该动作稳定映射为 `place_date_exception`，并新增完整 HTTP 回归，覆盖管理员登录、周一闭馆冲突、5 条节假日开放覆盖、4 条顺延闭馆、Revision 版本递增和审计事件。

已使用 `.local/research.db` 的临时副本启动隔离 API，按管理端真实 HTTP 契约重新调用相同接口，结果由 422 修复为 `HTTP 200`，Revision 正常递增；正式 12 条人工核验数据库未被写入，临时验证服务已停止。正式 API 已重启并继续使用 `.local/research.db`，修复已加载。

候选 Revision 详情同步完成一轮视觉和信息架构收口：顶部三个大型事实/体验/治理表合并为业务状态总览与“地点资料”，证据核验、冲突处理、求解资格和发布待办直接显示，内部地点/Revision 编号收进折叠技术信息；发布准备默认只展示前三项高优先级阻断，其余折叠。O05 拆为“常规开放规则与固定场次 / 固定闭馆日 / 日期例外”三个独立区块，日期预览改为紧凑工具条，说明性长 Alert 改为悬停帮助或区块副标题，所有操作列固定在表格最右侧。节假日生成弹窗按“生成依据 / 开放时间”分组并使用两列布局；单日例外根据类型动态显示字段，临时关闭不再要求或提交时间，开放/场次覆盖必须填写开始和结束时间。

自动化验证：后端全量 pytest `393/393`，admin-web Vitest `34/34`，TypeScript typecheck、production build 和 `git diff --check` 全部通过。浏览器已打开真实页面，但管理会话因页面刷新回到登录页；Chrome 扩展当前不可用。待取得本次输入 `.env` 本地管理员凭证的操作时确认后，完成登录后的视觉验收和弹窗只读检查；在用户明确确认写入前，不在正式 12 条数据库点击“确认生成”。

### 2026-09-01 续接进度（Revision 来源记录维护闭环）

当前节点保持 `M1 后段 / Gate 7 / OM1 / G7-R0.2-07`。首批多地点人工审核暴露的 P0 来源维护缺口已补齐：管理端 Revision 详情新增独立“来源证据”能力，数据编辑员可以从系统已审核的中文来源渠道中选择渠道，填写具体 HTTPS 页面、实际观察时间和采集方式；内容哈希与补充说明收进高级追溯区。Registry ID、Registry/字段字典哈希、目标阶段、来源决策、来源记录 ID 和创建时间全部由服务端自动写入，业务审核员不再接触 JSON、内部 ID 或治理哈希输入。

后端新增来源渠道只读目录、Revision-scoped 来源记录新增与解除关联接口。来源 URL 会按已审核域名、HTTPS、采集方式和凭证参数进行 fail-closed 校验；和风天气等只支持非地点字段的渠道不会进入地点来源下拉。来源记录保持不可变，纠错采用“新增替代来源 → 子证据改用新来源 → 从当前 Revision 移除旧来源”；移除只解除当前 Revision 关联，不物理删除历史来源。若 active 几何、访问点、开放时间、闭馆日、日期例外或地点关系仍引用旧来源，服务端返回 `source_record_in_use` 并列出中文引用类型，管理端提前禁用按钮并解释处理顺序。

新增/移除均受 candidate 生命周期、`expected_revision_version`、权限和 operation intent 幂等保护；成功后递增 Revision 版本，清除既有求解/审核资格、重置来源冲突状态并写 `PLACE_SOURCE_RECORD_CREATED` / `PLACE_SOURCE_RECORD_DETACHED` 审计。审核详情上下文只读，不显示来源新增/移除操作。O04/O05/O07 的来源下拉和表格优先显示“中文渠道名 + 观察日期”，内部来源编号只保留在悬停追溯信息中。

自动化验证进行中：新增后端回归覆盖渠道过滤、治理字段自动写入、幂等、敏感 URL 拒绝、版本递增、引用阻断、非物理删除和审计；前端回归覆盖 Revision-scoped API、中文业务展示、引用保护和 reviewer 只读。下一步是执行全量 pytest/Vitest/typecheck/build，启动真实 `.local/research.db`，用站内浏览器完成“新增来源 → 保存 → 在证据下拉可选 → 引用后禁止移除”的完整视觉验收并补充报告。该能力域完成验收前暂不判断提交。

### 2026-09-02 续接进度（来源维护浏览器验收准备）

本地 API 与管理端已重新启动，健康检查 `/health/live` 与 `/health/ready` 均通过；站内浏览器已登录真实 `.local/research.db`，进入候选修订 `revision-hz-cand-007`。页面已确认“来源证据”卡片、中文来源渠道/类型/采集方式、来源 URL 与观察时间，以及“新增来源记录”弹窗和高级追溯字段收起状态均可见；审核上下文的只读分支仍按代码和自动化测试保留。浏览器首次打开旧热更新页面时出现过动态模块加载失败，刷新后恢复，未发现新的运行时加载错误。

浏览器验收发现修订详情 `Descriptions` 在窄屏断点下的固定跨列设置会产生 Ant Design 列跨度告警，已移除固定 `span=3`，改为自然响应式换行；前端 typecheck、Vitest `32/32` 和 production build 已再次通过。用户确认后已在 `revision-hz-cand-007` 提交一条官方 HTTPS 来源记录：页面显示新增记录，数据库确认 `revision_version` 从 1 递增到 2、来源已关联，并追加 `PLACE_SOURCE_RECORD_CREATED` 成功审计事件；O04 几何来源下拉可见新的中文渠道标签，active 证据引用保护仍生效。来源维护能力域的代码、自动化、浏览器写入闭环和审计核对均已完成，可以进入提交评审。

### 2026-09-02 续接进度（O05 时间录入体验与跨午夜标记）

用户验收发现周规则与固定场次的开始、结束、最晚入园字段要求填写分钟数，不利于业务审核员理解。管理端现改为直接录入当地 `HH:mm`，并为开始、结束、最晚入园分别提供“次日”标记；例如 `23:00–次日 02:00` 自动转换为 `1380–1560` 分钟。API、数据库、求解器和既有发布快照继续使用分钟数稳定契约，不发生数据模型迁移；页面表格和指定日期预览统一以 `HH:mm`/“次日”展示。新增客户端转换与跨午夜顺序校验，防止未勾选“次日”导致结束时间早于开始时间。

本轮已补充前端单测：正常时间转换、跨午夜转换和缺失次日标记拒绝；下一步需完成浏览器中 O05 新增/编辑弹窗的视觉验收，并用一个隔夜场次数据验证保存请求仍发送分钟值。完成该能力域浏览器验收后，再与当前未提交的管理端来源维护和审计可读性改动一起判断提交边界。

### 2026-09-01 续接进度（O15 审计中心业务可读性与账号检索）

当前节点保持 `M1 后段 / Gate 7 / OM1 / G7-R0.2-07`。针对业务审核员无法理解审计列表中 `admin_actor_*` 和 `ADMIN_*` 技术编码的问题，O15 审计中心已将主列表的操作者改为管理员登录账号，将 30 余种当前及兼容动作统一映射为明确中文描述；稳定英文动作码和操作者内部编号仍保留在数据库、API 和展开的只读追溯区，不改变审计证据的稳定性。操作者角色在追溯区同步中文化，未知未来动作统一显示“其他管理操作”，不会在业务列表直接泄露机器码。

审计 API 正式新增只读 `actor_login_name` 字段和同名筛选参数。账号筛选与统一关键字现在通过 `admin_audit_events` 和 `admin_actors` 关联，在 `limit/offset` 分页前执行，`total` 与结果页保持一致；原有 `actor_id` 和英文 `action` 参数继续兼容。管理端操作者筛选改为登录账号输入，动作筛选改为中文可搜索下拉，提交给 API 的值仍为稳定动作码。展开详情显示中文角色，同时明确标注“操作者内部编号”和“动作编码”，兼顾业务阅读与技术追溯。

自动化验证结果：后端全量 pytest `385/385`；admin-web Vitest `29/29`，TypeScript typecheck、production build 和 `git diff --check` 均通过。按用户确认，站内浏览器等同于本轮 Google Chrome 验收：真实 `.local/research.db` 登录后，列表显示 `local.admin`、动作显示“管理员登录/初始化首个管理员”；按账号和中文动作组合查询后 URL 为 `actor_login_name=local.admin&action=ADMIN_SESSION_CREATE`，结果 `7/7`；展开详情可见安全管理员、内部编号和动作编码。验收报告见 `docs/test/reports/g7-r0.2-07-o15-audit-business-labels-browser-2026-09-01.md`。

本轮属于管理端“列表检索与业务可读性”能力域的补全，不改变当前里程碑和下一主线。下一步仍按首批候选审核准备度处理普通点状景点、区域/路线和固定场次三类真实候选，形成多地点人工审核 → Projection → 批次发布闭环；本轮 O15 修复适合与当前尚未提交的首批候选审核准备度工作台一起形成较大的提交边界。

### 2026-09-01 续接进度（首批候选审核准备度工作台）

当前节点保持 `M1 后段 / Gate 7 / OM1 / G7-R0.2-07`，工作重点已从单条页面修补转为首批 12 条真实杭州 candidate 的批量人工审核准备。候选清单 API 和管理端新增六项只读审核准备度：基础事实、来源与冲突、地点几何、访问点、开放时间和关系检查；列表顶部汇总本页待补录、可送审、审核中/可通过数量，单行显示已准备项数，展开后以中文区分“待补录 / 待审核 / 已核验”。准备度不会自动改变 Revision、审核任务、求解资格或发布状态。

准备度状态机已按正式流程收口：无开放审核任务时，即使六项证据均已核验也只能显示“可送审”；审核任务处于 `changes_requested` 时优先显示“待修改”；只有开放审核任务中六项均已核验才显示“可审核通过”。子证据必须由当前 Place 的 active 来源支持；Provider 候选点不能冒充地点几何；全天开放地点可以不建普通开放规则，但 active 闭馆日/日期例外仍须逐项核验；演出必须恰好一条固定场次；关系必须完成裁决并人工核验，无关系时必须登记完成关系检查。

候选导入同时修复历史占位语义：新导入 Revision 直接保留候选目录 `review_flags` 并以 `relation_review_status=pending` 开始关系检查；重跑脚本只对仍处于第 1 版、candidate、未审核未发布、不可求解且时长仍为 `1/1/1` 的纯净旧导入补回缺失元数据，不覆盖人工修改。已在执行前备份 `.local/research.db`，dry-run 返回 `updated=12`，正式回填返回 `updated=12`，再次 dry-run 返回 `skipped=12`；核对 12 条均保持 candidate、`solver_eligible=false`，页面应把 `1/1/1` 显示为“未采集”，没有越过审核或发布门禁。

自动化验证结果：后端全量 pytest `385/385`；新增准备度边界测试 7 项、候选导入/修复测试和候选列表 HTTP 契约回归均通过；admin-web Vitest 更新为 `26/26`，TypeScript typecheck、production build、相关 Ruff 检查和 `git diff --check` 均通过。API 与管理端已使用 `.local/research.db` 启动，readiness 正常，迁移版本为 `0013_backfill_solver_eligibility`。

按用户确认，站内浏览器验收等同于本项目本轮 Google Chrome 验收。已使用 `.env` 本地管理员凭证登录并验证：首页候选 12/待办 0/已发布 0；候选页显示待补录 12、可送审 0、审核中/可通过 0；全部未采集时长显示“未采集”；点状导入几何为 `1/6` 且“待审核”，区域/路线 Provider 点不冒充正式几何并保持 `0/6`；六项清单、查询、分页和详情跳转正常。验收期间发现并修复一个质量标记技术码泄露、一处固定场次阻断说明与 0 条规则自相矛盾、一个响应式 Descriptions span 控制台警告；修复后新的验收时间窗为 `0 error / 0 warning`。报告见 `docs/test/reports/g7-r0.2-07-candidate-review-readiness-browser-2026-09-01.md`。

下一步：开始按准备度处理首批 12 条中的第一组真实候选，至少覆盖普通点状景点、区域/路线和固定场次三类，目标是形成一个多地点人工审核 → Projection → 批次发布的完整能力域，而不是为每条候选单独提交；第一组稳定后再扩至全部 12 条并评估 50–75 条研究目录扩批。

### 2026-09-01 续接进度（结构化几何证据表单）

修复业务审核员需要直接填写 GeoJSON/JSON 的可用性缺口。管理端 Revision 详情的 O04 几何证据新增/编辑入口现在使用中文业务表单：点状地点填写经度、纬度；区域逐行填写边界坐标（至少 3 个点，系统自动闭合）；路线逐行填写轨迹坐标（至少 2 个点，按行进顺序）。提交前同时执行表单校验和转换层校验，统一检查经纬度范围、坐标数量与格式，并由系统生成标准 Point/Polygon/LineString GeoJSON。几何证据表格同步显示“点位坐标 / 区域边界点数 / 路线轨迹点数”等中文业务摘要，不再直接展示原始对象。

已有几何编辑时，系统会将可识别的标准 GeoJSON 反向拆解为表单字段；原始图形数据仅放在默认收起的只读追溯区，不再作为业务录入入口。来源记录继续通过当前地点来源下拉选择，页面提示使用中文解释，不要求审核员填写内部编号。新增前端回归覆盖点、区域闭合、路线最少点数、非法坐标中文错误和已有几何回填；点坐标范围校验也在转换层保留，避免绕过控件时产生非法载荷。

验证结果：admin-web Vitest `24/24`、TypeScript typecheck、production build、后端 pytest 全量和 `git diff --check` 已通过；站内浏览器已登录真实 research 数据检查点/区域/路线表单、原始数据默认收起和中文摘要，页面无新增控制台 error/warning。Chrome 扩展通道仍不可用，尚待 Chrome 可用时重复正式复核。当前节点仍为 `M1 后段 / Gate 7 / OM1 / G7-R0.2-07`，本轮属于 O04 业务可用性增强，尚未单独提交；需与浏览器验收证据及当前真实研究目录准备能力域一起判断提交边界。

### 2026-09-01 续接进度（研究目录边界审计门禁）

为推进 R0.2-07 数据隔离，新增只读目录审计工具 `scripts/audit_catalog_boundaries.py` 及 `travel_agent.data_governance.catalog_audit`。工具在研究环境准备前扫描已发布 Revision，识别显式 `browser_fixture` 来源、`browser-e2e` 地点命名空间、Browser E2E 名称和乱码业务标签；发现任何污染时返回退出码 `2`，不会删除、停用或改写数据库记录。candidate/human_verified 数量同时输出，便于审核目录规模核对。

对当前 `.local/travel_agent.db` 的审计结果为：`published_revision_count=3`、`published_business_count=1`，发现 2 条 Browser E2E published fixture，因此审计按预期 fail closed；这两条历史记录仍保留，待迁移到隔离测试数据库或隔离数据集。新增审计测试 2 项，后端全量 pytest 已达到 `379/379`；本轮未使用 SQL 修改任何业务状态。

当前节点仍为 `M1 后段 / Gate 7 / OM1 / G7-R0.2-07`。下一步是基于审计结果建立独立测试数据库/研究数据库边界，再以 10–15 个真实杭州地点执行批量人工审核、Projection、发布和 Chrome 用户端可见性回归；审计工具应作为后续 R0.3 受控部署前置检查。该能力尚未单独提交，待与数据隔离和小批次准备形成一个完整可验收模块后提交。

### 2026-09-01 续接进度（隔离数据库初始化能力）

新增 `scripts/init_isolated_catalog_db.py` 和 `travel_agent.data_governance.isolated_database`，用于创建 `test` 或 `research` 专用 SQLite 数据库。命令要求目标路径与本地业务库不同，目标已有表时拒绝覆盖，完成当前 Alembic head 迁移后写入 `<database>.environment.json` 环境标识；不会复制或改写业务数据。该边界让管理端本地验收库、自动化测试库和研究数据准备库可以独立运行，后续 MySQL/Compose 环境沿用同一“环境显式声明 + 迁移到 head”原则。

已在临时 research 数据库完成迁移 smoke test，版本为 `0013_backfill_solver_eligibility`；新增隔离目标校验测试 3 项。后端全量 pytest 已达到 `382/382`，`git diff --check` 通过。Browser E2E fixture 后续已按确认流程清理并保留备份，正式研究目录审计已恢复通过。

当前仍处于 `M1 后段 / Gate 7 / OM1 / G7-R0.2-07`。本轮已完成“只读污染识别 + 空隔离库初始化”代码闭环，尚缺真实地点导入和研究库数据迁移；下一步进入 10–15 个真实杭州地点的小批次人工审核/发布，不在本轮直接处理现有业务库中的历史 fixture。

### 2026-09-01 续接进度（Browser E2E fixture 清理）

根据确认请求，已对本地 `.local/travel_agent.db` 执行一次性、事务性 Browser E2E fixture 清理。操作前生成备份：`var/backups/travel_agent-before-browser-fixture-purge-20260901.db`。清理范围严格限定为 `browser-e2e-place` 及 `browser_fixture` 来源关联数据，包括 3 条测试 Revision（含基线）、2 个测试 Projection、1 个历史发布批次、1 个研究快照、审核任务/决定、证据子记录、测试关系/排除成员和对应测试审计事件；真实 `place-hz-cand-*` 数据未被选中或修改。

清理后核对：Browser E2E 地点数为 `0`，published Revision 为 `1`（真实“西湖音乐喷泉表演”），目录边界审计 `passed=true` 且违规数为 `0`；`candidate=71`、`human_verified=5`。全量 pytest `382/382` 通过，数据库迁移版本和用户端发布数据正常。备份可用于恢复被清理的本地测试数据，但不会自动恢复到业务库。

一次性清理脚本已按要求移除，仅保留清理结果和备份；后续 test/research 数据库应优先使用独立初始化命令，不再把 Browser E2E 数据写入业务验收库。当前节点仍为 `M1 后段 / Gate 7 / OM1 / G7-R0.2-07`；本轮数据隔离能力域已完成，下一步转入 10–15 个真实杭州地点的小批次人工审核、Projection、发布和 Chrome 可见性回归。

### 2026-09-01 续接进度（真实杭州候选小批次初始化）

为避免一次性把 72 条未经审核数据带入研究环境，扩展 `scripts/import_candidate_revisions.py` 支持重复 `--candidate-id` 和正整数 `--limit`；未传参数时仍保持原有全量导入行为。已在独立 `.local/research.db` 完成当前 Alembic head `0013_backfill_solver_eligibility` 初始化，并导入首批 12 条 `hz-cand-001`～`hz-cand-012`，状态核对为 `candidate=12`、`published=0`、地点数 12；目录边界审计通过。

该 research 库只作为真实数据审核准备库，12 条记录尚未人工补齐几何、访问点、开放时间、关系裁决，也未生成 Projection，不能进入用户端或研究快照。下一步在管理端针对这 12 条执行证据核验和 reviewer 审核，完成后再验证批次发布及 Chrome 用户端可见性；稳定后再扩充到 50–75 条。新增导入选择测试 2 项，相关回归已通过。

本轮已按要求移除一次性 Browser E2E 清理脚本，仅保留目录审计和隔离数据库初始化能力。已启动本地管理端/API 供小批次人工操作：API `http://127.0.0.1:8000` readiness 正常，admin-web `http://127.0.0.1:5173` 可访问，当前运行数据库为 `.local/research.db`；管理员使用 `.env` 中现有本地凭证登录。该运行实例只提供 12 条 candidate，未发布地点不会出现在用户端。

### 2026-09-01 续接进度（单条真实地点纵向闭环核对）

本轮对最新提交基线、项目路线文档和本地数据库进行了只读核对。确认真实景点“西湖音乐喷泉表演”第 6 版已经完整走通：有效来源、人工核验几何、人工核验访问点、固定场次、关系“无关系”裁决、Revision 审核通过、求解 Projection 发布、审计事件和用户端可见性均有对应记录。该事实证明 OM1 的单条人工核验—发布纵向流程可行，但不代表规模化研究目录或 Gate 7 外部验证已完成。

数据库当前概况：`place_revisions` 为 `candidate=71`、`human_verified=6`、`published=3`；`place_review_tasks` 为 `approved=9`、`closed=1`；`solver_place_projections` 为 `candidate=4`、`published=3`；已发布研究快照 1 个。3 条 published Revision 中，2 条是 `Browser E2E` 历史验收数据（含乱码区域/名称），1 条是真实业务景点。历史验收记录没有被删除或改写，后续必须迁移到隔离测试数据库或隔离数据集，避免进入研究目录和用户验收环境。

当前节点仍为 `M1 后段 / Gate 7 / OM1 / G7-R0.2-07`，但工作重点已从“证明单条发布流程可行”转为“规模化研究数据准备与环境隔离”。下一工作包按以下顺序执行：

1. 建立 local/dev/test/research 数据边界，迁出 Browser E2E published fixture，清理乱码展示数据；不通过手工 SQL 把 candidate 改为 `human_verified/published`，不破坏不可变审计和快照记录。
2. 选取 10–15 个真实杭州地点做第一批人工审核发布 dry run，验证来源、几何/访问点、开放时间、O07 裁决、Projection、批次发布和用户端可见性。
3. 小批次稳定后扩展到 Gate 7 研究最低目录约 50–75 个 `human_verified` Place，并补齐主要地点组合的真实高德 OD；随后进入 R0.3 服务器 Docker Compose 部署和 R0.4 内部 dry run。

本轮未修改运行代码、未修改数据库、未提交新 commit；当前仅有本状态文档和路线图的待提交更新，运行代码工作区保持干净。该状态不能被解读为 OM1、Gate 7、M1 MVP 或生产上线已完成。

跨里程碑稳定路线、阶段依赖和完成定义见 [项目完整路线图](project-roadmap.md)；本文件保留每轮执行账本和最新续接点。

### 2026-09-01 续接进度（已发布地点目录与管理端整体视觉升级）

修复“地点发布后从待发布列表消失，但管理端无处查看”的信息架构缺口。发布中心现拆分为“待发布地点 / 已发布地点”双视图：待发布视图继续展示 `human_verified` 修订版本的发布门禁、单条发布和批次预览/执行；已发布视图通过同一后端分页契约读取 `published` 修订版本，展示景点名称、地址、区域、中文类型/分类、数据版本、发布状态和发布时间，并支持关键字、区域和地点类型查询。单条或批次发布成功后会自动切换到已发布目录，让操作人立即看到结果；URL 使用 `?view=published` 保留可直达语义。

管理端整体视觉系统同步升级：重构深青绿数据治理侧栏、品牌标识、环境说明和选中态；顶部栏增加当前页面上下文、城市/里程碑标识和管理员身份块；统一全局色彩、圆角、阴影、按钮、表格、卡片、状态标签和筛选面板。管理首页重做为“候选地点 / 审核待办 / 已发布”数据概览，在已存在发布数据时不再错误显示“研究数据尚未发布”，而是明确展示已发布数量和直达目录入口。候选、审核、发布、审计和管理员页面的筛选区也已统一为同一视觉模式。

验证结果：admin-web Vitest `18/18`，新增发布中心回归测试验证从待发布切换到已发布后请求 `lifecycle_status=published`、展示中文业务字段且不泄露内部 Revision ID；TypeScript typecheck、production build 和 `git diff --check` 通过。Google Chrome 真实验收确认首页已显示“已发布 3 个”和直达入口；已发布目录真实返回 3 条数据，其中“西湖音乐喷泉表演”显示第 6 版、已发布和发布时间；关键字查询后稳定返回 1 条，页面无 `place_revision_*` 可见文本，待发布批次操作仍正常。Chrome 控制台 `0 error / 0 warning`。

当前仍处于 `M1 后段 / Gate 7 / OM1 / G7-R0.2-07`。本轮“已发布数据可见性 + 管理端视觉升级”已形成完整可验收能力域，建议与前述管理检索、O07 和发布目录改动一并作为较大提交。下一步仍是清理/隔离 Browser E2E 和问号乱码 fixture，准备受控部署所需的真实人工核验目录。

### 2026-09-01 续接进度（管理端列表查询与审核/发布业务字段可读性）

完成候选地点、地点审核、发布中心、审计中心和管理员与角色五个管理页面的列表检索闭环。地点类列表统一支持名称/地址/分类等关键字、行政区域和地点类型；管理员支持登录名/编号、账号状态和角色；审计在原有精确技术条件上增加统一关键字。所有后端检索都在 `limit/offset` 分页前构造 SQL 条件，同时返回筛选后 `total`，不使用“仅筛当前前端页”的伪查询。

地点审核与发布中心主表不再向审核员展示 `place_revision_*` 和审核任务内部 ID，改为展示景点名称、区域、中文地点类型、中文分类、“第 N 版”和状态/门禁。内部 Revision ID 仍保留在 API 契约和前端行数据中，仅用于稳定详情路由、审核决定和发布操作。发布批次预览同步返回业务摘要，并将历史英文分类 `museum` 统一显示为“博物馆”；数据库仍可保留稳定英文枚举。

验证结果：后端全量 pytest `370/370`；新增回归覆盖候选/审核的关键字、区域、类型、分页前筛选与业务摘要，以及管理员/审计的筛选后总数。admin-web Vitest `17/17`、TypeScript typecheck、production build和 `git diff --check` 全部通过。Google Chrome 实际验收已逐页验证五个查询入口；候选地点“龙井村”查询返回 2 条名称/地址匹配，审核页不存在关键字返回空结果，发布页“西湖音乐喷泉”仅返回对应版本，管理员“local.admin”返回 1 条，审计“ADMIN_LOGIN”返回筛选后总数。审核/发布页均确认无 `place_revision_*` 可见文本。同时迁移 Ant Design 已弃用弹窗属性，干净 Chrome 标签页控制台为 `0 error / 0 warning`。

当前仍处于 `M1 后段 / Gate 7 / OM1 / G7-R0.2-07`。本轮“管理端列表检索与业务可读性”能力域已完整收口，达到适合与当前 OM1/O07/发布目录可见性改动一并提交的节点。本地历史 fixture 仍存在问号乱码内容，属于既有测试数据编码债务，不影响本轮查询契约，但不应随受控部署数据进入人工评审环境。下一步建议在提交后继续 G7-R0.2-07 的发布数据隔离/编码清理和受控部署前数据准备。

### 2026-09-01 续接进度（O07 识别规则审计与发布目录进入用户端）

完成 O07 自动识别实现审计：当前系统不是实时通用空间/语义推理引擎，而是由杭州候选目录构建管道登记 11 组关系线索，再由导入流程写入 `place_relations`；`candidate_catalog.py` 负责关系类型、端点存在性、ID 唯一性和待裁决状态校验，O07 页面负责人工裁决或修订级“确认无关系”。新增《地点关系自动识别规则（O07）》文档，明确名称归一、实体/关系去重、`contains` / `part_of` / `overlaps` / `same_experience` 语义、杭州 11 组实际线索、发布门禁、误报/漏报处理和后续几何拓扑增强边界。评审结论：现有“已登记线索的结构校验与发布阻断”逻辑正确，但“任意地点关系都由程序自动识别”的表述不准确，后续必须补充通用候选生成算法和置信度。

修复管理端发布成功但用户端不可见的数据源割裂：本地开发组合根新增数据库已发布目录适配器和动态版本提供器，按城市聚合每个 `place_id` 最新的 `published` Revision/Projection，读取人工核验访问点、开放时间、闭馆日和求解字段，转换为既有 `PublishedSolverData`；candidate、仅 human_verified、已合并/停用地点和同一地点旧发布版本均不进入用户端。数据库没有已发布数据或不可用时继续使用明确标注的 7 景点本地 Fixture。数据库目录当前 OD 仍使用有标记的近似路网，生产环境继续使用严格不可变 JSON/真实高德 OD，不因本地适配放宽生产门禁。

验证结果：后端全量 pytest `377/377`，其中新增“数据库发布后 `/api/v1/attractions` 可见”回归测试；杭州关系目录校验测试通过；admin-web Vitest `16/16`、TypeScript typecheck、production build、`git diff --check` 通过。用户端 H5 已从首页进入景点选择页，确认最新发布的“西湖音乐喷泉表演”可见且可选、未回退旧 7 景点 Fixture、控制台 error 为 0；未发布地点未进入列表。数据库中的历史发布验收数据 `Browser E2E Revision 3` 因确为 published 也会显示，后续应迁到隔离测试数据库。Chrome 已启动且扩展/本机配置检查正常，但本轮 Chrome 控制通道仍未建立，正式 Chrome 复核尚未完成，站内浏览器验收报告见 `docs/test/reports/g7-r0.2-07-published-catalog-user-visibility-2026-09-01.md`。当前处于 M1 后段 / Gate 7 / OM1 / G7-R0.2-07，本轮能力域代码已收口，Chrome 验收缺口需如实保留。

### 2026-09-01 续接进度（演出地点开放规则门禁语义修复）

复核 `place_revision_e59759e1f65648498d2bd41745fdc316` 时确认：该地点类型为“演出/固定场次”，当前存在 1 条来源有效且已人工核验的 `opening_hours` 规则，但没有 `fixed_session` 规则。此前发布门禁将此情况错误返回为 `TIME_RULE_UNRESOLVED`，容易让审核员误以为开放时间核验失效。

现新增稳定原因码 `FIXED_SESSION_REQUIRED`：演出、灯光秀等地点必须提供明确开始/结束时间的固定场次；普通开放时间已核验不会替代固定场次。管理端 O05 对演出地点增加明确提醒，发布阻断摘要提供“处理固定场次（O05）”入口，并说明需要编辑规则或新建修订版本后重新送审。更新 ADR-0018、API 契约和中文原因码映射，新增求解门禁回归测试。

同时保留 O05 人工核验进度总览，显示有效记录数与已核验记录数，避免“有记录”和“满足地点类型要求”被混淆。当前 API 对该 Revision 返回 `FIXED_SESSION_REQUIRED` 与 `PLACE_NOT_SOLVER_ELIGIBLE`，其中前者是本次问题的直接原因。

验证结果：后端全量 pytest `376/376`、admin-web Vitest `16/16`、TypeScript typecheck、production build、`git diff --check` 均通过；本地 API 已重启并实测返回新原因码。浏览器当前已登录并可打开管理端，但本轮未通过浏览器提交任何修订数据；登录后视觉验收仍需确认 O05 固定场次提示和阻断摘要入口。当前仍处于 M1 后段 / Gate 7 / OM1 / G7-R0.2-07，未提交。

### 2026-08-31 续接进度（发布阻断摘要结构化与来源冲突处理入口）

修复 Revision 详情页“发布阻断摘要”只显示笼统文案、无法定位和处理的问题。页面加载修订版本时同步读取来源冲突和发布门禁检查结果：阻断项现在按“原因、影响、处理建议、证据区域入口”展示，并将缺少几何、访问点、开放时间、来源记录、关系裁决、人工核验和求解资格等条件映射到 O04/O05/O06/O07 或审核操作区。发布门禁原因码统一通过中文标签展示，不再把内部代码直接暴露给审核用户。

新增 O06 来源冲突卡片，展示来源组、来源记录、来源地址、来源决策、观察时间和状态；数据编辑员可填写裁决依据并通过现有并发控制接口标记冲突已处理，reviewer 仅可查看。即使系统没有发现具体冲突，仍提供“确认无冲突并完成裁决”入口，避免 `conflicts_resolved=false` 造成无处理路径的发布阻断。处理后明确提示修订版本会递增、审核/求解资格会清除、需要重新送审。更新管理 API 类型与封装，补充来源冲突查询/处理请求测试。

验证结果：后端全量 pytest 通过；admin-web Vitest `16/16`、TypeScript typecheck、production build、`git diff --check` 通过。站内浏览器已确认当前管理端登录页可访问，但因管理员密码属于敏感凭证，本轮未代填登录，未完成登录后真实 candidate 的视觉验收；待用户在管理端登录后复核阻断卡片和 O06 处理入口。当前仍处于 M1 后段 / Gate 7 / OM1 / G7-R0.2-07，完整能力域尚未提交。

### 2026-08-31 续接进度（O05 状态可解释性与核验路径简化）

针对用户看到“开放时间尚未核验”但认为页面已有已核验规则的问题，核对当前 URL 对应 Revision `place_revision_e59759e1f65648498d2bd41745fdc316` 的真实 API/SQLite 数据：当前版本为 `candidate`，O05 有 1 条开放时间规则但其 `review_status=candidate`，并非 `human_verified`；发布检查同时返回“尚未准备求解投影”。页面阻断说明现显示“当前读取到 N 条有效规则，其中 M 条已人工核验”，避免把记录存在误认为已完成核验。

为降低人工核验复杂度，Revision 详情新增“人工核验进度”总览，按 O04/O05/O07 显示“已核验/有效记录总数”，点击即可定位到对应区域；求解资格仍由证据核验、来源冲突/关系裁决和 Revision 级审核自动计算，不增加人工勾选项。原有逐项审核按钮和权限边界保持不变。

验证结果：admin-web Vitest `16/16`、TypeScript typecheck、production build、`git diff --check` 通过；未代填管理员密码，尚未完成登录后 Chrome/站内浏览器视觉验收。当前仍处于 M1 后段 / Gate 7 / OM1 / G7-R0.2-07，本能力域继续与前述阻断摘要改动合并，暂不提交。

### 2026-08-31 续接进度（Revision 证据继承与审核字段引导修复）

修复新建 Revision 只复制主记录、导致 Geometry/AccessPoint/TimeRule/Closure/DateException 全部为空的问题：现在新建 Revision 会复制基线 Revision 当前仍有效的子证据，为每条子证据生成新的 revision-scoped ID，并将审核状态重置为 `candidate`、清空 `reviewed_at`；已停用证据不会重新生效，旧 Revision 与审核记录保持不可变。新增后端回归测试验证证据数量、ID 隔离和重新核验门禁。

同步优化管理端证据录入：Geometry 类型改为中文下拉并提供 GeoJSON 示例及经纬度顺序说明；AccessPoint 用途改为入口/出口/路线/演出地点等中文选项；来源记录改为当前地点有效来源下拉，避免审核人手填内部 ID；时间字段改为业务可读的 `HH:mm` 输入，并以“次日”勾选明确跨午夜，页面自动转换为 API/数据库使用的分钟值；O04/O07 增加“几何、访问点、来源、求解投影端点、关系裁决”的非开发人员操作口径。关系区明确由归一/去重线索生成，当前页面负责裁决，不要求 reviewer 凭空创建关系。

验证结果：后端全量 pytest 通过，新增 `test_new_revision_copies_active_evidence_as_unverified_children` 通过；admin-web typecheck、production build 通过；站内浏览器已确认 O04 新增表单显示中文字段说明、GeoJSON 示例、访问点用途下拉、来源下拉和 O07 关系裁决提示。Chrome 控制通道本轮未连接，需后续 Chrome 可用时补做正式浏览器报告。当前仍处于 M1 后段 / Gate 7 / OM1 / G7-R0.2-07，未提交。

### 2026-08-31 续接进度（证据填写提示收敛为悬停图标）

根据审核人员反馈，将 O04/O05/O07 表单中始终展开的长说明改为字段标签旁黄色“!”提示图标，鼠标悬停后显示填写规则、示例和验证口径；保留错误校验和必要状态告警，不再让帮助文字挤压表单。管理端 typecheck 和 production build 均通过。站内浏览器页面刷新后因内存会话回到登录页，本轮未重新输入管理员密码，未完成登录后的悬停交互截图；待 Chrome/管理会话可用时补做正式可视化验收。当前仍处于 M1 后段 / Gate 7 / OM1 / G7-R0.2-07，未提交。

### 2026-08-31 续接进度（访问点新增 422 前置校验修复）

定位并修复访问点新增接口 `POST /api/v1/admin/place-revisions/{revision_id}/access-points` 的常见 422 触发路径：新增表单此前未将“访问点用途”标记为必填，且打开新增弹窗时没有默认用途，用户未选择用途会把空值提交到后端。现在新增访问点默认选择“游客入口”，用途字段增加前端必填校验和中文错误提示；后端原有 Pydantic 与领域校验保持不变，仍拒绝非法用途、坐标、来源和版本号。打开几何/访问点弹窗时同步清理上一次表单残留值。

验证结果：admin-web Vitest `14/14`、typecheck、production build 和 `git diff --check` 通过；API 服务已重启并保持就绪。当前仍处于 M1 后段 / Gate 7 / OM1 / G7-R0.2-07，未提交。

### 2026-08-31 续接进度（访问点 422 复现与前端错误可见性修复）

使用当前本地 SQLite 和 `.env` 管理员会话复现访问点接口：完整载荷可正常返回 `200`；此前页面 `422` 的原因是访问点用途或来源字段未实际选中时提交了空值。新增访问点现在默认带入“游客入口”和当前地点第一条有效来源，并对用途增加必填校验；管理 API 错误对象同步保留 `field_errors`，前端会把具体字段和校验消息展示出来，不再只显示笼统的“提交内容未通过校验”。为复现创建的临时访问点已通过正式停用接口清理。

验证结果：admin-web Vitest `14/14`、typecheck、production build、后端全量 pytest 和 `git diff --check` 通过；API `/health/ready` 正常。当前仍处于 M1 后段 / Gate 7 / OM1 / G7-R0.2-07，未提交。

### 2026-09-03 O17 同步任务过程流（G7-R0.2-09）

- **产品里程碑**：M1 后段；**证据阶段**：Gate 7；**管理侧**：OM1。
- **本轮完成**：同步任务新增 AI/系统执行过程记录流。后端按查找官方公告、读取正文、AI 提取、确定性校验、预览生成、发布提交等节点追加带时间的中文消息；终态保留完整记录和最后阶段。
- **管理端**：任务详情增加阶段进度和执行过程时间线，运行中自动刷新，失败/重试/取消后仍可回看；不向业务用户暴露内部状态码或 JSON。
- **验证**：后端全量 pytest 422/422；admin-web Vitest 36/36；TypeScript typecheck、Vite build 通过。
- **当前工作**：O17 过程可观测性已完成，等待真实配置下的站内浏览器验收。
- **下一步**：启动 API/Worker，使用已配置凭证创建一次 preview 任务，验收过程流的实时追加、失败提示和完成后的历史回看；通过后与 O17 其他改动一起形成大模块提交。

### 2026-09-03 O17 预览数据复核与发布入口

- **本轮完成**：预览校验通过时保存本轮 AI 结构化的节假日区间、调休上班日期和官方原文依据；任务详情新增“本轮预览数据”审核区，避免预览结果只有版本号而不可查看。
- **交互闭环（已修订）**：业务人员可在独立确认弹窗中增删或修改节假日段、调休日期和官方原文依据；点击“确认内容并入库发布”后，不再重新调用 AI 或创建第二个抓取任务，而是提交调整后的结构化数据。后端沿用原预览任务的官方来源，再次执行年度、日期、冲突、七类节日完整性和证据校验，通过后在同一事务内直接创建不可变发布版本和审计记录。
- **验证**：后端 O17 定向测试通过；admin-web 测试、TypeScript typecheck、production build 通过。
- **当前工作**：等待使用真实 2024 预览任务进行浏览器验收，确认明细表和确认发布入口可见且操作语义清晰。
- **补充修正**：预览明细改为独立“本轮预览数据确认”弹窗；任务详情只提供进入按钮，避免内容过长。旧任务若没有明细会明确提示由旧 Worker 创建，需要重新预览，不再显示空表误导用户。
- **错误修复**：原“确认”按钮通过拼接任务 ID 创建 `sync` Job，operation intent 可能超过后端 64 字符上限并返回 422，且用户调整内容无法进入契约。现改为专用 `POST .../{job_id}/confirm`，前端使用短 UUID 意图；状态机支持 `validated_preview → published`，同一确认意图可安全重试。页面会显示后端中文确定性校验原因。第三方性能采集脚本抛出的 `startTime` 异常不属于业务调用栈，不再由失败的旧创建任务路径触发。
- **验证状态**：后端全量 pytest `424/424`，O17 定向用例包含“人工修正预览后发布”和相同确认意图幂等；管理端 Vitest `36/36`、TypeScript typecheck、production build、目标范围 Ruff 和 `git diff --check` 通过。首次全量回归出现一次 Windows `WinError 10055` 临时套接字资源错误，同一用例隔离复跑及第二次全量回归均通过，判定为非业务回归。
- **当前工作**：本地 API 已重启，新确认端点已出现在 OpenAPI 且 readiness 为 `0015 / ready=true`。站内浏览器因刷新后管理会话失效停在登录页，等待人工登录后验收编辑控件、增删操作与中文校验提示；最终发布属于有副作用操作，浏览器验收不代替业务人员确认发布。
- **问题修正**：使用 AI 同步发布的新年度日历生成日期例外时，原流程因强制要求当前地点存在同 ID 的来源记录而返回 422。现将已发布年度日历自身的官方来源记录作为合法依据，并在管理端提供该来源选项；普通时间证据仍继续要求当前地点的 active 来源记录。新增 `HolidayCalendar.source_record_id` 贯通数据库日历目录、O05 生成接口和页面选择器，避免业务人员复制或伪造来源记录。
- **管理端统一错误反馈**：`AdminApi` 现对所有标准非成功响应和网络异常触发统一通知边界。`400/403/409/422` 以“操作受限”警告展示后端中文业务原因、字段信息和请求编号，`401` 显示登录失效，`5xx/网络失败` 显示错误通知；通知使用固定 key，轮询或并发错误会更新现有提示而不会连续刷屏。页面内原有错误区继续保留供回看，但各按钮无需再单独实现弹窗。新增 API 回调和 Provider 实际通知测试，全量 admin-web Vitest `39/39`、TypeScript typecheck 与 production build 通过。
## O07 关系检查闭环（本轮）

已补齐地点关系与裁决的“无关系”正式闭环：新增修订级 `relation_review_status` 和迁移 `0012_relation_review_status`；新建/编辑候选进入待检查状态，数据编辑员可在无关系记录时确认“无关系”，操作带并发版本、幂等意图和审计，reviewer 只读。已有关系仍须逐条裁决。发布门禁会对待检查且无关系记录的修订返回 `RELATION_REVIEW_REQUIRED`。O06 页面明确冲突由系统自动发现、由数据编辑员处理；所有证据表格的操作列统一置于最右侧。
### 2026-09-01 续接进度（退回修改后重新送审未进入审核队列）

修复送审状态流转缺口：当候选 Revision 已存在 `changes_requested` 的开放审核任务时，原实现直接原样返回旧任务，导致审核工作台默认的“待审核”筛选无法显示该任务。现在再次送审会在原任务上执行乐观锁保护的状态迁移 `changes_requested → ready_for_review`，递增任务版本、更新时间并保留完整决定历史；`in_review` 等进行中任务仍不会被覆盖。新增接口回归测试，验证重新送审后可通过 `review_status=ready_for_review` 查询到任务。

验证结果：后端审核接口测试通过，admin-web Vitest `16/16`、TypeScript 类型检查和 `git diff --check` 通过。当前仍处于 M1 后段 / Gate 7 / OM1，工作区尚未提交。

### 2026-09-01 续接进度（O07 无关系进度与求解资格修复）

修复 O07 已确认无关系时进度仍显示 `0/0` 的问题：管理端将“无关系且已确认”展示为 `1/1 已核验`，并明确这只代表 O07 检查完成，不等于整条 Revision 已具备求解资格。进一步修复审核通过事务此前未写入 `solver_eligible=true` 的缺口；新增迁移 `0013_backfill_solver_eligibility` 为历史 `human_verified/published` 修订回填资格，后续 reviewer 通过时同步写入。当前本地数据库已升级到 0013，健康检查为 ready；发布门禁仍继续检查真实来源、几何、访问点、开放规则、固定场次和投影完整性。
## 2026-09-02 O05 节假日开放与闭馆顺延（G7-R0.2-08）

- **阶段**：M1 后段 / Gate 7 / OM1，首批多地点人工审核准备。
- **本轮完成**：新增版本化中国大陆法定节假日历目录（2025、2026）；新增管理员 API 查询日历并按日历批量生成节假日开放和节后顺延闭馆日期例外；生成记录保留现有来源、candidate 状态、逐项审核和审计门禁。
- **求解器兼容**：继续消费 `PlaceDateException`；数据库和分钟字段不迁移；已发布数据通过 `open_on_dates/closed_on_dates` 进入运行时可用性判断。
- **业务场景**：浙江省博物馆孤山馆区可维护周二至周日 09:00–17:00、16:30 最晚入馆、周一闭馆，再用年度节假日历生成法定节假日开放和节后第一天闭馆记录。
- **验证**：新增节假日历单测通过；管理端 TypeScript typecheck 和生产构建通过；现有管理端、目录回归测试通过。浏览器视觉验收待服务启动后进行。
- **下一步**：补充节假日生成 API 集成测试和指定日期预览原因展示（“法定节假日开放”“节后顺延闭馆”），然后进行整体验收并在大模块完成后提交。

### G7-R0.2-08.1 人工验收修正

- 修复节假日历接口不可用时按钮仅置灰且无解释的问题：按钮现在显示加载状态或“加载节假日历”，失败时展示具体错误和重新加载入口。
- 明确日期例外同时支持“临时关闭、开放覆盖、场次覆盖”，界面直接展示判定优先级，不再要求审核员理解内部解析码。
- 确定优先级：临时关闭 > 日期开放/场次覆盖 > 固定闭馆日 > 普通周规则；同一日期只允许一条有效日期例外，历史异常数据仍按临时关闭优先确定性解析。
- 修正节假日顺延口径：仅当节假日范围实际包含固定闭馆星期时，才生成该日开放覆盖及该段节假日结束后第一天的顺延闭馆，避免每逢节假日都机械闭馆和产生大量无意义审核记录。
- 本地人工核验运行库已切换为 `.local/research.db`（12 个地点、12 个修订版本：11 candidate、1 published）；`scripts/run_local_dev.py` 现在默认遵循 `.env` 的 `TRAVEL_AGENT_DATABASE_URL`，显式 `--database` 仍可临时覆盖，避免重启后误回到 72 地点的 `travel_agent.db`。


### 2026-09-11 O05：show 多场次与候选时间规则删除（H3/C2/C4/C6）

需求来自用户：show 从恰好一条固定场次放宽为至少一条，由求解器自动选可行场次；候选入口删除开放规则/固定场次，移除停用，审核入口保留停用且停用记录仍可见、不参与校验。ADR-0025 覆盖唯一场次旧限制。

实现涉及审核准备度/发布门禁、带来源的场次投影、数据库与 JSON 适配器、快照脚本、OR-Tools 离散域、独立约束校验、用户端与分享、候选删除 API 和审计。删除只影响 candidate 当前行，有权限/版本/幂等/同事务资格重置，不影响历史版本；旧 DELETE API 保持停用语义。显式场次日期改用全天联合求解，暂不执行 DAY_SPREAD/普通节点时长扩展；无显式场次输入保持原行为。无新依赖声明、无迁移、未操作实际研究库、未进行 Git 写操作。

环境说明：本机 Python 3.12 + PYTHONUTF8=1，未使用 uv.lock 锁定环境；默认 python 在续接时转为无 pytest 的 Conda，后续显式指定 Python312。OpenAPI 在当前未锁定环境生成了无关响应类型差异，因此生成类型文件只合入新端点及其 operation 块，保留既有生成类型；锁定环境应复核全量生成。定向 mypy 新模型/场次解析/投影载荷通过；routing 扩大检查仍有既存 OR-Tools 无类型桩与 Any 返回问题。

操作偏差：为补齐仓库已声明的 frontend 测试依赖执行 npm install，事前说明会写入 node_modules/npm 缓存，但未提前列出删除清单。npm 实际新增 87 包、移除 71 包；未符合删除前清单确认纪律，已当场告知用户。package.json/package-lock.json 无差异；未删除源码、文档或研究数据库。后续不得把依赖安装视为无删除副作用。

验证结果：pytest 515/515（58.30s）；Golden 8/8（`var/reports/gate6-golden-show-sessions-20260911.json`）；admin-web 47/47 + typecheck + production build；frontend 19/19 + tsc；ruff、layering（123 文件零违规）、API 契约（86 登记/83 实现，code_only=0）、sync/check_docs 全过。新增覆盖多 show、跨午夜、入园截止、缺 OD、返程、日期覆盖、停用来源、删除幂等/版本/生命周期、确认交互、发布快照/数据库场次读取和分享脱敏。已实现，尚未提交、部署、用户验收。


### 2026-09-11 S4/S8 CI 收尾与响应契约首片（H3）

用户确认按顺序推进。先核对 O05 已提交 6e9e463 并销账，再完成 CI 锁定环境/frontend Vitest：固定 uv 0.8.15，`uv sync --locked --extra dev` 检查锁文件一致性，后续 --no-sync；frontend npm ci 携带既有 lock 生成方式 legacy-peer-deps，保留 job check 名称。backend schema artifact 交给 admin-web 重生成并检查版本库差异。

本地 uv 工具安装到全新 var/tools/ci-uv-0815，先 dry-run 确认新建环境、仅安装 55 包，再同步至全新 var/venvs/ci-locked-20260911；未刷新已有 node_modules、未改锁文件、未操作研究库。Python 3.12 锁定环境原基线 515/515（56.62s）与 Golden 8/8 通过，随后才开始响应模型首片。

响应模型首片覆盖 34 个路由：修订详情与共用修订返回的写操作、候选列表/准备度、证据、审核任务/送审/单项决定/列表、发布检查和时间预览。模型只属于 HTTP 层，不修改领域业务用例。17 个前端类型从 schema 再导出；修复旧测试夹具缺少实际必返字段；时间预览正确表达常规场次 ID 和日期覆盖 ID 联合。响应省略/default/null 与字符串时间保持兼容，extra=forbid 防止意外内部字段随成功响应泄漏。新增 6 个响应契约用例。

验证：锁定环境 pytest 521/521（59.69s）、Golden 8/8、ruff、layering（124 文件零违规）、新增响应模型 mypy、API 契约（86/83，code_only=0）、sync/check_docs；管理端 47/47 + typecheck/build、用户端 19/19 + typecheck 全过。使用相同锁定 schema 独立再生成文件，SHA-256 两次一致（814bbe4d156e5d3b7e96b605652cb27fd126a132cb1ae3cbd040ddd429904b78）。Workflow YAML 已解析检查。真实 Linux runner、artifact 跨 job 链路及首次远端 CI 尚未执行，需要用户提交触发；本轮未进行 Git 写操作。

状态：上述两片实现完成待提交。响应模型仍有认证/来源冲突/批量审核/发布批次/O17 等后续切片；S7 数据批次和 S8-4 R0.3 部署前提仍未满足。未进行依赖升级，无新业务依赖或迁移。


2026-09-11 提交前复核与本地启动：用户要求判断是否适合提交并启动全部服务。本轮改动具备提交边界，diff --check 通过，沿用此前锁定环境 521/521、Golden 8/8 及两端验证结果，未重复无必要测试。按现有代理配置启动 API 8000/8001、admin-web 5173、H5 10086、O17 worker；页面和两端代理健康检查均 200，两个运行 API 的 OpenAPI 均包含 PlaceRevision 响应模型，H5 编译成功、worker 启动日志确认。启动前以 SQLite mode=ro 确认研究库迁移已达 0015；之后正常应用初始化和队列处理属于用户本次启动授权。未启动本机 MySQL/Redis，未执行 Git 提交/推送。启动日志 var/reports/services-20260911-170900，模块日志仍在 logs。尚待远端首次 CI 与用户功能验收。


### 2026-09-11 S7 前置核对与依赖梳理（H3）

核对 436bf20 已进入 dev，开场工作区干净，收口上一轮待提交账本。用户声明人工批准完成；只读报告及 SQLite mode=ro 复核原定 12 条批次为 10 published / 2 candidate，027 杭州博物馆和 052 良渚博物院均仅有原始 candidate 版本。048 钱江新城灯光秀已 published/solver_eligible，但 4 条 active/human_verified 场次均最晚入场晚于开始 10 分钟，当前 ADR-0025 校验不通过，准备度 5/6。全库 Revision 13 published / 59 candidate / 2 retired。未改任何数据；已询问用户批准范围/环境与远端 CI 结果（gh 未登录）。

已梳理审核门面的全部方法、调用依赖与共享事务助手；时间规则/日期例外需独立拆分以符合单文件 800 行目标，发布退役仍留在既有发布事务中。仅更新账本及拆分准备说明，业务实现等待硬前提差异澄清。无 Git 写操作、无服务重启、无数据库写入。


### 2026-09-11 CI OpenAPI 契约测试隔离修复（H3/S8-2）

用户补充：人工批准批次排除杭州博物馆 027 与良渚博物院 052；远端 frontend 通过、backend 519 passed/2 failed、admin-web 因 needs: backend 跳过。日志定位两项测试隐式依赖被忽略的 var/reports/openapi-schema.json，本地历史产物掩盖了缺陷；CI 正式 schema 导出在 pytest 之后，且输出到 var/ci。用户明确批准按提议范围修复与回归。

增加 function-scoped schema_path fixture 调用真实导出脚本写入 tmp_path，检查测试显式传 --schema；当前契约通过要求 exit 0/status ok/code_only=[]；缺契约测试给定有效 schema 并断言错误来源；只读检查连续执行两次，逐次确认成功和输入内容不变。无需修改 CI 顺序或提交生成产物。

验证：专项 7/7；Python 3.12 uv.lock 隔离环境全量 521/521（86.72s，48 项现有弃用警告）；ruff CI 范围通过、修改文件格式通过、分层 124 文件零违规；sync/check_docs 与 diff --check 通过。仅测试和文档改动，未重跑无关前端/Golden，未改业务实现、依赖、研究库，未执行 Git 写操作。实现完成待用户提交推送并验证远端。


### 2026-09-12 S7-1 准备度职责迁移（H3）

开场 HEAD 为 cec78fd、工作区干净，用户确认远端全绿；此前已确认批准批次排除 027/052。前两轮搬移失败源于助手错误的文本范围和字符索引，并非发现循环依赖，纠正此前归因。上轮未经列清单确认执行 Remove-Item 删除临时 review_readiness.py，违反 git-safety 删除纪律；本轮已向用户如实说明，本轮未删除文件。

本片通过 AST 定位，只迁移 evaluate_review_readiness 与 _readiness_check；新模块显式依赖领域类型、has_source_content_conflict 和 valid_session_timing，不反向依赖审核门面。迁移前后两个函数与原模块其余函数/类的 AST 一致。旧公共导入和私有别名保持兼容；离线 research_readiness 报告改为直接使用新模块；既有准备度测试迁到新入口并新增旧导入身份兼容回归。

验证补充：专项 14/14；Golden 8/8；管理端 47/47、tsc、Vite 生产构建通过（输出新 var/reports/s7-readiness-admin-20260912，emptyOutDir=false）；用户端 19/19、tsc 通过；ruff 全 CI 范围、分层 125 文件零违规、API 契约 86/83/code_only=0，通过。OpenAPI 与既有快照仅 x-exported-at 时间戳不同。后端首轮 521 passed/1 setup error（85.58s），为测试客户端创建 socketpair 时 WinError 10055；该用例单独复跑通过，继续串行全量核实。未删除文件、未操作研究库、无 Git 写操作。

串行全量最终结果：522 passed（96.79s），48 项已有弃用警告，无失败。sync/check_docs、git diff --check 通过。准备度首片完成待提交，S7-1 整体仍进行中；原门面 3998→3764 行、新模块 243 行。


### 2026-09-12 S7-1 来源服务与窄仓储接口（H3）

用户要求继续 S7。开工 HEAD cec78fd，准备度首片未提交，保留原改动并扩大 In-flight 登记。将来源冲突查询/裁决、渠道查询、来源新增/解除 5 个用例原样迁至 ReviewSourceService；审核门面保持原签名显式委托。共享 ReviewSupport 只持有 clock/ids 与权限/actor/重放/事件/校验助手，审计仍在用例事务内。ReviewRepository 分成 3 个 Protocol 并保留兼容聚合；来源服务依赖窄 SourceReviewUnitOfWork（2 个修订方法、4 个来源仓储方法），不接触审核任务或发布仓储能力。

新增两项 HTTP→服务→真实临时 SQLite 回归：审计 add 后抛异常时 revision/source/audit 一起回滚；恢复后相同 intent 能成功重试，随后重放不递增版本或追加审计。原来源集成测试通过。新模块 mypy 通过；对原 review.py 使用 --shadow-file 验证 HEAD 存量 28 错误，当前剩余 22 错误均为原非来源路径类型债（准备度迁出另带原有 6 项类型债），不宣称全仓类型检查通过。源用例 AST 与原版一致，转发签名一致，OpenAPI 与准备度片快照仅导出时间不同。

本片最终验证：锁定环境 pytest 524/524（97.96s）、Golden 8/8、admin-web 47/47 + tsc + Vite 生产构建（新 var/reports/s7-source-admin-20260912，emptyOutDir=false）、frontend 19/19 + tsc，ruff 全 CI 范围、分层 128 文件零违规、API 契约、sync/check_docs 与 diff --check 全通过。无依赖/迁移变化，无研究库写入、文件删除或 Git 写操作。本轮与未提交准备度首片共同形成可审阅阶段切片；S7-1 其余职责待继续。


### 2026-09-12 S7-1 几何/访问点及共享证据事务（H3）

开工 HEAD 9cc9792、工作区干净，核对准备度/来源提交并销账。新增 ReviewGeometryService（6 用例）、EvidenceMutationSupport（泛型证据事务与两个 helper），GeometryReviewUnitOfWork 显式窄化仓储；同一个事务实现继续供旧门面时间证据写入复用，不复制审计/版本/错误转换。AST 校验 6 用例、原其他方法、事务方法体一致，门面签名一致，OpenAPI 仅导出时间不同。review.py 3362→3103，新模块361/124，接口235行。

新增6项HTTP+临时SQLite回归覆盖几何/访问点新增编辑停用：审计add后失败时全状态回滚（包括review_flags），相同intent重试成功，重复重放状态不变。专项6/6及既有证据相关4项通过；新模块与ports mypy通过，旧门面仍为原22项类型债，未扩大业务修改。无数据库迁移/依赖变更，不操作研究库，不删除文件、不执行Git写操作。

验证：后端全量530/530（111.81s）、Golden8/8、ruff CI范围与修改文件格式、分层130文件零违规、API契约86/83/code_only=0，均通过。OpenAPI仅导出时间变化。用户端19/19与tsc通过；管理端验证收尾。

最终收口：管理端47/47、tsc与生产构建通过（新输出var/reports/s7-geometry-admin-20260912，禁用目录清理）；用户端19/19与tsc通过。sync/check_docs与git diff --check通过。本片完成待提交，S7-1关系/时间/审核任务/发布等仍待拆分。


### 2026-09-12 S7-1 关系职责切片（H3）

开场HEAD e293a56、工作区干净；上一片已提交并销账。关系裁决与无关系确认原样迁入200行ReviewRelationService，门面保留原签名；RelationReviewUnitOfWork收窄关系仓储至2方法，证据摘要共享移至review_evidence。review.py3103→2954行。AST/签名/OpenAPI一致。新增两项事务回归，关系表/修订表/审计表在审计异常时全部回滚，失败后同intent重试通过，无关系确认重复重放通过。测试最初按应用层candidate断言，但既有仓储明确落盘pending，核对后修正断言，不改业务。

发现HEAD已有成功重放缺陷：resolve_relation事件target_id为relation_id，重放使用该ID读取revision导致404；本片纯搬移保留，登记独立修复风险，不能宣称关系裁决成功重放已通过。类型检查关系/证据/接口模块通过；ruff、分层131文件零违规、Golden8/8、API契约通过。未改研究库、迁移或依赖，无文件删除或Git写操作。

最终验证：532/532（75.02s）、Golden8/8、admin47/47/tsc/build、frontend19/19/tsc、ruff/分层131文件/API/文档门禁通过。成功关系重放缺陷已在var/reports临时库中分别用迁移方法及git show HEAD的原方法对照，两者均404；未固定为预期错误测试、未改变业务。本片完成待提交，时间/审核任务/发布等仍待拆分。


### 2026-09-12 关系裁决成功重放修复（H3）

开场HEAD b595ecd、工作区干净。将resolve_relation成功重放的revision查询从existing.target_id改为请求revision_id；后者已受digest一致性校验保护，不改审计目标或历史事件。扩展既有关系事务用例验证成功重放200、版本/状态不变，以及相同intent修改decision_note返回admin_operation_intent_conflict/409。

验证：专项2/2；全量532/532（64.16s）；冲突断言补充后专项再次通过；ruff全CI范围、分层131文件零违规、相关模块mypy、sync/check_docs、diff --check通过。未新增测试数量，仅扩展断言；本轮未重跑前端或Golden，之前结果属于上一片。无数据库写入、迁移、依赖变更、文件删除或Git写操作。


### 2026-09-12 S7-1 时间证据职责切片（H3/O05）

开场核对 HEAD 38d3518、工作区干净；关系 b595ecd 与重放修复 38d3518 已提交，账本销账。将时间规则新增/修改/删除/停用、固定闭馆日新增/修改/停用、日期例外新增/修改/停用、节假日例外生成及内置日历目录迁至 review_time.py（776行）；只读日期预览迁至 review_time_preview.py（245行），避免拆出新的超800行模块。TimeReviewUnitOfWork 的 catalog 收窄为11方法；继续复用 EvidenceMutationSupport，版本、权限、审计与幂等事务保持。门面2954→2348行，原方法AST与保留签名对照一致，旧日历导入兼容。

新增11项HTTP/临时SQLite事务回归：三类时间证据新增/修改/停用、候选时间规则删除、节假日一次生成9条日期例外。在审计写入后模拟失败，验证修订/时间规则/闭馆日/日期例外/审计全部回滚；同intent重试成功、重放全状态不变、改变reason_code返回409。已有时间/节假日专项8项通过。原show多场次、删除与停用、未核验证据过滤和节假日规则不改动。

最终验证：Python3.12锁定环境、PYTHONUTF8=1，pytest543/543（70.35s），Golden8/8；ruff CI范围、修改文件格式、分层133文件零违规、API契约86/83/code_only=0；时间/预览/接口模块mypy通过。admin-web47/47+tsc+生产构建，frontend19/19+tsc。首轮全量未设置PYTHONUTF8，两项脚本测试stdout为None；设置后相关17项与完整全量均通过，不改业务代码。全仓mypy存量债保留。

报告输出var/reports/s7-time-*20260912，管理端构建emptyOutDir=false。未执行Git写操作、文件删除、研究库写入、依赖变更或迁移；测试按预告使用框架临时目录。已实现并完成自动回归，未进行本片部署/用户验收，远端CI须提交后确认。S7-1仍进行中，下一片审核任务，再推进发布及修订生命周期/门面收口。


### 2026-09-12 S7-1 审核任务职责切片（H3）

接续上一轮占位模块和窄化Protocol，HEAD为c4fa989。使用AST定位并包含staticmethod装饰器，迁入list_tasks/count_tasks/list_decisions/get_task/submit/decide/decide_batch及两事务辅助方法；9方法AST对照不变，门面签名保留转发。review_tasks519行，review.py2348→1996行。TaskWorkflowRepository复用任务8方法并增加get_revision/approve_revision，TaskEvidenceRepository只读证据。未改审核门禁、逐项提交、审计或重放语义。

新增6项回归：首次送审/退回重送/通过/退回/取消在审计插入后失败，修订、任务、决策与审计整体回滚；相同intent重试成功，重放不增版本或审计，不同载荷409。应用层批量首项失败、后项成功；重试复用已成功结果，每项最终仅一条决策/审计。构造多修订测试时修正fixture固定place_id造成的唯一键冲突，不修改业务。

发现原有HTTP批量模型不含task_id且extra=forbid，应用服务却要求task_id，无法提交有效批量任务ID。本片保留接口，应用层批量测试通过，HTTP缺口单独待修。mypy原22项中4项随原AST迁入任务模块，门面剩18项，不宣称类型债清零。

最终：549/549（76.60s）、Golden8/8、ruff、134文件分层零违规、API86/83/code_only=0、admin47/47+tsc+build、frontend19/19+tsc。报告与构建写var/reports/s7-tasks-*20260912，构建禁用目录清理。未执行Git写操作、文件删除或研究库写入，无依赖/迁移变更；已有暂存占位模块保留用户暂存状态，最终工作区更新需用户重新暂存。已实现和自动回归，未部署/人工验收；S7-1其余发布和修订生命周期职责待拆分。


### 2026-09-12 批量审核HTTP输入契约修复（H3）

HEAD929d62a，审核任务片已提交并销账。原BatchDecidePlaceReviewInput复用不含task_id的单项模型，导致前端正确提交task_id仍422。新增BatchDecidePlaceReviewItemInput继承单项字段并要求1–64字符task_id；单项端点继续从路径取ID。API契约明确每项ID/独立intent，生成api-schema.d.ts仅新增批量模型及引用变化。原批量事务回归改走真实HTTP，覆盖某项审计失败/后项成功和重试无重复裁决审计；新增缺失/空/超长ID422且无审计写入3项。

验证：专项9/9、全量552/552（99.99s）；ruff、分层134文件零违规、契约86/83/code_only=0；admin47/47+tsc+生产构建。本地缺已声明openapi-typescript，使用npx临时同版本7.13.0生成，无package/lock改动。Golden与frontend沿用前片，未独立重跑；类型债保留。本片已实现及自动回归，未部署或人工验收；后续继续发布服务拆分。无Git写操作、文件删除、研究库写入或迁移变更。构建写var/reports/batch-http-admin-20260912且emptyOutDir=false。


### 2026-09-12 S7-1 发布职责切片（H3）

接续HEAD21535b2，批量审核HTTP契约已提交。将publication_check、prepare_projection、publish_revision、preview_publication_batch、execute_publication_batch、list/get_research_snapshots迁入review_publication.py（640行），门面1996→1568行；PublicationUnitOfWork窄化为17个目录方法及修订只读接口。保留发布门禁、show固定场次投影、批次逐项状态、研究快照hash与幂等重放逻辑，门面方法签名与HEAD保持。

新增4项事务回滚测试：投影准备、单项发布、批次预览、批次执行在审计add后失败时，修订/投影/批次/批次项/快照全部无残留；成功重试与重放不重复写入。发布专项4/4及既有投影/发布测试通过。

最终验证：pytest556/556（100.23s）、Golden8/8、ruff、分层135文件零违规、API86/83/code_only=0、发布模块mypy；admin47/47+tsc+build，frontend19/19+tsc。未改研究库、迁移、依赖，无Git写操作/删除。构建写var/reports/s7-publication-admin-20260912且emptyOutDir=false。账本已更新，本片完成待提交；后续为修订生命周期及门面收口。


### 2026-09-12 S7-1 修订生命周期职责切片（H3）

创建/编辑修订与证据核验迁至review_revision.py，437行；门面1568→1228行，3方法AST/签名保持。窄化RevisionLifecycleUnitOfWork，复制有效证据、资格重置、版本校验及审计事务保持。新增3项HTTP回滚/重放回归。pytest559/559、Golden8/8、ruff、分层、契约、两端测试/构建通过；修订模块保留18项原有mypy类型债。未改研究库、迁移、依赖，无Git写操作。


### 2026-09-12 S7-1 查询职责切片（H3）

将7个修订/准备度/仪表盘/详情查询迁至review_queries.py，查询Protocol不含写入或审计能力；门面1228→约900行。新增4项读取请求重复执行状态不变回归。pytest563/563、Golden8/8、ruff、分层137文件、API契约、admin47/47+tsc+build、frontend19/19+tsc通过，查询模块mypy通过。账本已更新，下一步门面收口；无Git写操作、删除、研究库/迁移/依赖变更。


### 2026-09-12 S7-1 门面收口评估

准备度、来源、几何/访问点、关系、时间、审核任务、发布、修订生命周期和查询职责均已拆出并提交；PlaceReviewWorkflowService保留显式兼容转发方法，当前约1027行，超过≤800行退出标准。为保持公共签名、OpenAPI和调用方兼容，本轮不采用动态代理；后续可用显式facade mixins继续收口。ruff、分层、check_docs及四组新模块专项回归通过，未执行Git写操作。下一步进入S7-2 HTTP路由拆分。


### 2026-09-12 S7-2 O05请求模型归位（H3）

将四个O05时间证据Pydantic输入模型移至admin_time_models.py，admin.py显式导入，路径/权限/响应和OpenAPI保持。pytest563/563（87.45s）、ruff、分层137文件、check_docs通过。无研究库/迁移/依赖变更，无Git写操作或删除；下一步拆O05路由注册。


### 2026-09-12 S7-2 O05路由注册拆分（H3）

O05时间证据路由（时间预览、规则/闭馆日/日期例外增改删、节假日生成及证据审核）迁至admin_o05.py的register_o05_routes函数；admin.py显式调用，避免循环导入。既有管理端HTTP回归通过，ruff、分层139文件、check_docs通过。无API路径/权限/响应变化，无研究库/迁移/依赖变更，无Git写操作或删除；下一步拆O04路由。
