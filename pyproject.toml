[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "travel-agent"
version = "0.1.0"
description = "Deterministic itinerary solver for the travel assistant M1 MVP"
requires-python = ">=3.12"
dependencies = [
    "alembic>=1.13,<2",
    "fastapi>=0.111,<1",
    "httpx>=0.27,<1",
    "ortools>=9.10,<10",
    "pymysql>=1.1,<2",
    "python-dotenv>=1.0,<2",
    "redis>=4.6,<5",
    "sqlalchemy>=2.0,<3",
    "uvicorn>=0.30,<1",
]

[project.scripts]
travel-agent-archive-logs = "travel_agent.observability.archive:main"
travel-agent-holiday-worker = "travel_agent.infrastructure.holiday_sync.worker_runtime:main"

[project.optional-dependencies]
dev = [
    "mypy>=1.11",
    "fakeredis>=2.24,<3",
    "pytest>=8.3",
    "ruff>=0.6",
    "types-redis>=4.6,<5",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
addopts = "-q"
pythonpath = ["src"]
testpaths = ["tests"]

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.mypy]
python_version = "3.12"
strict = true
files = ["src", "tests"]
