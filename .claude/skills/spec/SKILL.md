---
name: spec
description: 把产品假设/功能需求转成带 Given/When/Then 验收标准的需求规格，并建立与假设编号的追溯（G2 阶段）
---

# 需求规格化技能

把「功能」转成「可验证的规格」，是工作流 G2 阶段的核心动作。

## 输入

- 产品文档中的功能清单（[docs/product/](../../../docs/product/)）
- 假设登记册（[docs/assumptions.md](../../../docs/assumptions.md)）

## 步骤

1. **选功能**：按优先级（核心假设承载者优先，如「一键生成分日行程」承载 H3）。
2. **写头标**：关联假设 `H-x`、关联指标、优先级。
3. **拆验收标准**：用 Given/When/Then，每条标准必须回答「测谁、怎么测、对应哪个假设」。
4. **把主观词客观化**：遇到「合理」「好用」等词，拆成可测子指标（硬约束违反数、体力不超预算、通勤耗时、开放时间命中率）。
5. **写追溯**：在规格末尾列出「假设 ↔ 验收标准 ↔ 指标」映射表。
6. **落盘**：写入 `docs/specs/<功能名>.md`。

## 质量自检（退出准则）

- [ ] 每条验收标准可测、有量化阈值
- [ ] 核心假设的验收标准已客观化（不依赖纯主观评分）
- [ ] 追溯表完整（每个 H-x 至少被一条规格覆盖）

## 产出示例路径

`docs/specs/trip-solver.md`
